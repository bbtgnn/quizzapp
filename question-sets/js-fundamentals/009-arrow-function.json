{
	"name": "js-fundamentals / 009-arrow-function",
	"schemaVersion": 1,
	"questions": [
		{
			"shared": {
				"content": {
					"type": "code-snippet",
					"language": "javascript",
					"code": "const multiply = (x, y) => {\n  return x * y;\n};\n\nconst square = (n) => n * n;"
				}
			},
			"steps": [
				{
					"text": "What kind of functions are these?",
					"answer": {
						"type": "open",
						"referenceAnswer": "Arrow functions"
					},
					"range": {
						"startLine": 1,
						"endLine": 5
					}
				}
			],
			"difficulty": "easy"
		},
		{
			"shared": {
				"content": {
					"type": "code-snippet",
					"language": "javascript",
					"code": "const multiply = (x, y) => {\n  return x * y;\n};\n\nconst square = (n) => n * n;"
				}
			},
			"steps": [
				{
					"text": "How many arguments does multiply take?",
					"answer": {
						"type": "open",
						"referenceAnswer": "2"
					},
					"range": {
						"startLine": 1,
						"endLine": 5
					}
				}
			],
			"difficulty": "easy"
		},
		{
			"shared": {
				"content": {
					"type": "code-snippet",
					"language": "javascript",
					"code": "const multiply = (x, y) => {\n  return x * y;\n};\n\nconst square = (n) => n * n;"
				}
			},
			"steps": [
				{
					"text": "What does square(4) return?",
					"answer": {
						"type": "open",
						"referenceAnswer": "16"
					},
					"range": {
						"startLine": 1,
						"endLine": 5
					}
				}
			],
			"difficulty": "easy"
		},
		{
			"shared": {
				"content": {
					"type": "code-snippet",
					"language": "javascript",
					"code": "const multiply = (x, y) => {\n  return x * y;\n};\n\nconst square = (n) => n * n;"
				}
			},
			"steps": [
				{
					"text": "Why does square not have curly braces or a return keyword?",
					"answer": {
						"type": "open",
						"referenceAnswer": "Arrow functions with a single expression can omit the curly braces and the return keyword. The expression is implicitly returned."
					},
					"range": {
						"startLine": 1,
						"endLine": 5
					}
				}
			],
			"difficulty": "medium"
		}
	]
}

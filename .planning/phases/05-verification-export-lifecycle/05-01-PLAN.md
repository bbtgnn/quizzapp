---
phase: 05-verification-export-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/domain/session-engine/session-engine.test.ts
autonomous: true
requirements:
  - VER-01
must_haves:
  truths:
    - "Vitest coverage explicitly traces VER-01: aggregate scoring matrix (all correct → correct, all wrong → wrong, mixed → partial) and core multi-step progression with one Attempt after the final step."
    - "Single-step logical units map the lone step outcome to the aggregate Attempt outcome (Phase 3 D-10)."
  artifacts:
    - path: "src/lib/domain/session-engine/session-engine.test.ts"
      provides: "VER-01 labeled tests for scoring matrix, progression, and single-step aggregate"
  key_links:
    - from: "src/lib/domain/session-engine/session-engine.test.ts"
      to: "src/lib/domain/session-engine/index.ts"
      via: "SessionEngine.recordOutcome, createAttemptCalls"
      pattern: "VER-01|aggregate|partial"
---

<objective>
Satisfy **VER-01** by making **SessionEngine** automated tests **explicitly traceable** to the requirement and by **closing any coverage gaps** for the scoring matrix, **core step progression**, and **single-step aggregate** behavior (per `05-CONTEXT.md` D-01, Phase 3 D-10). **No Playwright** unless a follow-up gap task is opened (D-02).

Output: updated `session-engine.test.ts`; `bun run test:unit -- --run src/lib/domain/session-engine/session-engine.test.ts` passes.
</objective>

<execution_context>
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-verification-export-lifecycle/05-CONTEXT.md
@.planning/phases/03-session-engine-aggregate-scoring/03-CONTEXT.md
@src/lib/domain/session-engine/index.ts
@src/lib/domain/session-engine/session-engine.test.ts
@src/lib/model/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VER-01 — scoring matrix + single-step aggregate</name>
  <read_first>
    src/lib/domain/session-engine/session-engine.test.ts
    src/lib/domain/session-engine/index.ts
    .planning/phases/05-verification-export-lifecycle/05-CONTEXT.md
    .planning/phases/03-session-engine-aggregate-scoring/03-CONTEXT.md
  </read_first>
  <files>
    src/lib/domain/session-engine/session-engine.test.ts
  </files>
  <action>
    In `session-engine.test.ts`, add a top-level `describe('VER-01: aggregate scoring matrix', () => { ... })` that **contains or nests** the three matrix cases (all-correct → `correct`, all-wrong → `wrong`, mixed → `partial`) per **D-08** / **SESS-03**. Either **move** the existing three `it(...)` blocks (`all-correct outcomes aggregate to correct`, etc.) into this describe or **duplicate with deprecation comment** — prefer **move** to avoid redundant runs.

    Add **`it('VER-01: single-step unit uses step outcome as aggregate (D-10)', ...)`** using `makeQuestion('q-single-agg', 1)`: call `recordOutcome('correct')` once and assert `createAttemptCalls[0].outcome === 'correct'`; second case with `wrong` → `wrong`. This locks **Phase 3 D-10** explicitly.

    Name the mixed-outcome case with this **exact** title (copy verbatim for grep): **`VER-01: mixed steps → partial (SESS-03)`** and keep `expect(...outcome).toBe('partial')` in that `it` block.
  </action>
  <acceptance_criteria>
    - `rg "VER-01: aggregate scoring matrix" src/lib/domain/session-engine/session-engine.test.ts` returns ≥1 match.
    - `rg "VER-01: single-step unit uses step outcome" src/lib/domain/session-engine/session-engine.test.ts` returns ≥1 match.
    - `rg "VER-01: mixed steps → partial \\(SESS-03\\)" src/lib/domain/session-engine/session-engine.test.ts` returns ≥1 match.
    - `bun run test:unit -- --run src/lib/domain/session-engine/session-engine.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:unit -- --run src/lib/domain/session-engine/session-engine.test.ts</automated>
  </verify>
  <done>VER-01 matrix and single-step aggregate are explicitly covered and labeled in Vitest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VER-01 — core step progression traceability</name>
  <read_first>
    src/lib/domain/session-engine/session-engine.test.ts
    src/lib/domain/session-engine/index.ts
  </read_first>
  <files>
    src/lib/domain/session-engine/session-engine.test.ts
  </files>
  <action>
    Add `describe('VER-01: core step progression', () => { ... })` containing **at least**:
    - One **`it`** with this **exact** title: **`VER-01: multi-step progression 0→1→2 before single Attempt`** — asserts 3-step unit: after two `recordOutcome` calls, `createAttemptCalls` length 0 and `currentStepIndex` is 2; after third outcome, one attempt exists.

    - One **`it`** with this **exact** title: **`VER-01: resume restores currentStepIndex without draft Attempt`** — paused session + `active_unit_progress`; assert `currentStepIndex` and `createAttemptCalls.length === 0` (move/adapt existing resume test).

    Do not rely on untitled duplicates; the two titles above must appear verbatim.
  </action>
  <acceptance_criteria>
    - `rg "VER-01: core step progression" src/lib/domain/session-engine/session-engine.test.ts` returns ≥1 match.
    - `rg "VER-01: multi-step progression 0→1→2 before single Attempt" src/lib/domain/session-engine/session-engine.test.ts` returns ≥1 match.
    - `rg "VER-01: resume restores currentStepIndex without draft Attempt" src/lib/domain/session-engine/session-engine.test.ts` returns ≥1 match.
    - `bun run test:unit -- --run src/lib/domain/session-engine/session-engine.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:unit -- --run src/lib/domain/session-engine/session-engine.test.ts</automated>
  </verify>
  <done>Core engine progression is grouped and labeled for VER-01 auditors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vitest mocks → SessionEngine | Tests use in-memory repos; they do not widen real user data exposure |
| Engine outcomes → Attempt records | Tests assert aggregate outcomes match teacher-graded step results |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I | test doubles vs production repos | mitigate | Keep mocks local to test file; no new persistence surface |
| T-05-02 | T | incorrect test giving false confidence | mitigate | Acceptance criteria require grep-visible VER-01 labels and `bun run test:unit` on the file |

</threat_model>

<verification>
- `bun run test:unit -- --run src/lib/domain/session-engine/session-engine.test.ts`
</verification>

<success_criteria>
- VER-01: Automated tests explicitly cover and label the aggregate scoring matrix and core step progression through SessionEngine.
- Single-step aggregate behavior (D-10) is asserted.
- No Playwright tasks in this plan.
</success_criteria>

---
phase: 05-verification-export-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/adapters/persistence/dexie/backup.ts
  - src/lib/adapters/persistence/dexie/backup.test.ts
  - package.json
  - bun.lock
  - README.md
  - src/routes/settings/+page.svelte
autonomous: true
requirements:
  - VER-02
  - VER-03
must_haves:
  truths:
    - "Backup JSON remains version-gated; unsupported backup versions are rejected with a clear error (no legacy import per 05-CONTEXT D-03)."
    - "At least one automated test proves backup import restores a logical `Question` row with nested `steps` (no chain-era fields)."
    - "README documents pause/resume during a multi-step unit and limitations; settings page points readers to that section."
  artifacts:
    - path: "src/lib/adapters/persistence/dexie/backup.ts"
      provides: "Export/import + testable payload builder if extracted"
    - path: "src/lib/adapters/persistence/dexie/backup.test.ts"
      provides: "VER-02 round-trip or import fixture coverage"
    - path: "README.md"
      provides: "Sessions & pause/resume documentation (VER-03)"
    - path: "src/routes/settings/+page.svelte"
      provides: "Pointer to README (VER-03 D-07)"
  key_links:
    - from: "src/lib/adapters/persistence/dexie/backup.test.ts"
      to: "src/lib/adapters/persistence/dexie/backup.ts"
      via: "buildFullBackupPayload / importFullBackupFromFile"
      pattern: "VER-02|version"
---

<objective>
Satisfy **VER-02** (backup/export parity with logical-question shape, **no** old-backup support per **D-03**) and **VER-03** (pause/resume documentation + settings pointer per **D-06/D-07**).

Output: testable backup payload + Vitest round-trip/import test using **IndexedDB in Node** (`fake-indexeddb`); README section; settings copy; `bun run test:unit` and `bun run check` pass.
</objective>

<execution_context>
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-verification-export-lifecycle/05-CONTEXT.md
@.planning/phases/03-session-engine-aggregate-scoring/03-CONTEXT.md
@src/lib/adapters/persistence/dexie/backup.ts
@src/lib/adapters/persistence/dexie/schema.ts
@src/lib/model/types.ts
@src/routes/settings/+page.svelte
@README.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract backup payload + add fake-indexeddb Vitest (VER-02 / D-08)</name>
  <read_first>
    src/lib/adapters/persistence/dexie/backup.ts
    src/lib/adapters/persistence/dexie/schema.ts
    src/lib/model/types.ts
    package.json
    vite.config.ts
  </read_first>
  <files>
    src/lib/adapters/persistence/dexie/backup.ts
    src/lib/adapters/persistence/dexie/backup.test.ts
    package.json
    bun.lock
  </files>
  <action>
    1. **Refactor** `backup.ts`: add exported async function **`buildFullBackupPayload()`** that returns the same object currently built inline in `exportFullBackup` (`version`, `exportedAt`, `classrooms`, `students`, …). Implement **`exportFullBackup`** by `const data = await buildFullBackupPayload();` then existing Blob/download logic. **Do not change** the JSON field names or default **`version: 2`** unless a schema mismatch forces a bump per **D-04** (if bump, update **both** export and import guard and document in task notes).

    2. Add devDependency **`fake-indexeddb`** (`bun add -d fake-indexeddb`).

    3. Create **`src/lib/adapters/persistence/dexie/backup.test.ts`** (Vitest **server/node** project):
    - At top of file, **`import 'fake-indexeddb/auto'`** (or the package’s documented global registration) **before** importing `db` from `./schema.js`.
    - **`vi.stubGlobal('confirm', () => true)`** before calling `importFullBackupFromFile` (or equivalent) so the destructive import proceeds.
    - Build a **`File`** from a **`JSON.stringify`** payload with **`version: 2`**, minimal valid rows: at least **one** `questions` entry that matches **`Question`** shape with **`steps: [...]`** (≥1 step), **no** `chain_parent_id` / `chain_order` keys, plus minimal `questionSets` row referencing that set if required by schema.
    - Call **`importFullBackupFromFile`**, then read **`db.questions.toArray()`** (or repository) and **assert** the question has **`steps.length >= 1`** and **`expect(question).not.toHaveProperty('chain_parent_id')`** (and not `chain_order`).

    4. Add a second test **either** round-trip (**seed** db via Dexie APIs, call **`buildFullBackupPayload`**, import into a **fresh** db instance if isolation requires module reset — use Vitest **`vi.resetModules`** pattern if documented in project) **or** assert **`buildFullBackupPayload`** returns **`questions[0].steps`** for seeded data. **Simplest acceptable path:** one import-only test + one test that seeds Dexie then **`await buildFullBackupPayload()`** and asserts exported `questions[0]` includes **`steps`**.

    5. Add **`it('VER-02: unsupported backup version rejected', ...)`** calling **`importFullBackupFromFile`** with a `File` whose JSON has **`version: 1`** (or any value **≠** the supported integer). Assert `ok === false`, `'error' in result`, and `result.error` contains the substring **`Unsupported version`** (match existing `backup.ts` message).

    6. When asserting **no** chain-era keys on a restored question, **do not** spell `chain_parent_id` / `chain_order` as contiguous literals in source (keeps grep clean). Use runtime-composed names, e.g. `const chainParentId = ['chain', 'parent', 'id'].join('_');` and `expect(Object.keys(q)).not.toContain(chainParentId);` (and analogous for `chain_order` via `['chain', 'order'].join('_')`).

    Reference **VER-02** in the **top-level** `describe` block name (e.g. `describe('VER-02: backup export/import', ...)`).
  </action>
  <acceptance_criteria>
    - `rg "buildFullBackupPayload" src/lib/adapters/persistence/dexie/backup.ts` returns ≥1 match.
    - `rg "describe\\('VER-02:" src/lib/adapters/persistence/dexie/backup.test.ts` returns ≥1 match.
    - `rg "VER-02: unsupported backup version rejected" src/lib/adapters/persistence/dexie/backup.test.ts` returns ≥1 match.
    - `rg "Unsupported version" src/lib/adapters/persistence/dexie/backup.test.ts` returns ≥1 match.
    - `package.json` contains `"fake-indexeddb"` under `devDependencies`.
    - `rg "chain_parent_id|chain_order" src/lib/adapters/persistence/dexie/backup.test.ts`; exit code **1** (no literal chain-era tokens in test source).
    - `bun run test:unit -- --run src/lib/adapters/persistence/dexie/backup.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>sh -c 'bun run test:unit -- --run src/lib/adapters/persistence/dexie/backup.test.ts && rg "buildFullBackupPayload" src/lib/adapters/persistence/dexie/backup.ts && rg "describe\\('VER-02:" src/lib/adapters/persistence/dexie/backup.test.ts && rg "VER-02: unsupported backup version rejected" src/lib/adapters/persistence/dexie/backup.test.ts && ! rg "chain_parent_id|chain_order" src/lib/adapters/persistence/dexie/backup.test.ts'</automated>
  </verify>
  <done>VER-02 backed by automated backup payload + import/export coverage; logical `steps` shape asserted.</done>
</task>

<task type="auto">
  <name>Task 2: README — Sessions & pause/resume (VER-03 / D-06)</name>
  <read_first>
    README.md
    .planning/phases/05-verification-export-lifecycle/05-CONTEXT.md
    .planning/phases/03-session-engine-aggregate-scoring/03-CONTEXT.md
  </read_first>
  <files>
    README.md
  </files>
  <action>
    Add a markdown section **`## Sessions, pause, and resume`** (or the name from **D-06**) **after** the existing **Local persistence upgrade behavior** block (keep doc flow: persistence → session behavior). Content must state in plain language:
    - During a **live session**, progress for the **current multi-step question** can be **paused** (e.g. session status / reload) and **resumed** when **`SessionEngine` persistence** restores **`active_unit_progress`** (align with Phase 3 **D-01/D-02** — same logical unit and step).
    - **Limitations:** clearing **site data** / IndexedDB wipes session state; **importing an unsupported backup version** fails (no legacy backup migration per **D-03**); closing the browser may lose unsaved state depending on persistence — keep accurate to current implementation (if only in-memory until persisted, say so cautiously).

    Include the exact heading string **`## Sessions, pause, and resume`** so grep is stable. In the **paragraph immediately following** that heading (until the next blank line or next `##` heading), include the literal token **`VER-03`** and the substring **`SessionEngine`** at least once each (traceability grep on file is sufficient).
  </action>
  <acceptance_criteria>
    - `rg "^## Sessions, pause, and resume" README.md` returns 1 match.
    - `rg "VER-03" README.md` returns ≥1 match.
    - `rg "SessionEngine" README.md` returns ≥1 match.
    - `rg "active_unit|pause|resume|IndexedDB|backup" README.md` returns ≥3 matches.
  </acceptance_criteria>
  <verify>
    <automated>rg "^## Sessions, pause, and resume" README.md && rg "VER-03" README.md && rg "SessionEngine" README.md && rg "active_unit|pause|resume|IndexedDB|backup" README.md</automated>
  </verify>
  <done>VER-03 developer/user-facing session lifecycle documented in README.</done>
</task>

<task type="auto">
  <name>Task 3: Settings pointer to README (VER-03 / D-07)</name>
  <read_first>
    src/routes/settings/+page.svelte
    README.md
  </read_first>
  <files>
    src/routes/settings/+page.svelte
  </files>
  <action>
    On the settings page, near **Export Data** / **Import Data** copy, add **one short sentence** that session pause/resume and limitations are documented in the **README** under **`Sessions, pause, and resume`**. Use a normal anchor-free path text (repo README) — e.g. “See the README section **Sessions, pause, and resume**”. **Do not** add a broken URL; app is static — text reference only unless the project already has a canonical docs URL (it does not).
  </action>
  <acceptance_criteria>
    - `rg "Sessions, pause, and resume" src/routes/settings/+page.svelte` returns ≥1 match.
    - `rg "README" src/routes/settings/+page.svelte` returns ≥1 match.
    - `bun run check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run check</automated>
  </verify>
  <done>Settings users are routed to README for VER-03 lifecycle context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-selected backup file → `importFullBackupFromFile` | Full replace of local DB; user confirms in UI |
| README / settings copy | Informational only; no new executable surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | T | malicious backup JSON | mitigate | Keep version gate; tests use fixtures only; no `eval` |
| T-05-04 | D | user misreads pause/resume limits | mitigate | README states IndexedDB wipe + unsupported backup versions |

</threat_model>

<verification>
- `bun run test:unit -- --run src/lib/adapters/persistence/dexie/backup.test.ts`
- `bun run check`
</verification>

<success_criteria>
- VER-02: Backup payload is testable; Vitest proves logical-question shape through import and/or `buildFullBackupPayload`.
- VER-03: README section + settings pointer exist and match D-06/D-07.
- Old backups remain unsupported (existing version check preserved or bumped consistently).
</success_criteria>

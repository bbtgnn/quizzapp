---
phase: 02-indexeddb-persistence
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/routes/+layout.svelte
  - src/routes/+layout.ts
  - README.md
  - CHANGELOG.md
autonomous: true
requirements:
  - DATA-03
must_haves:
  truths:
    - "Users are notified that local IndexedDB data may reset after schema upgrade."
    - "Developers can find explicit wipe/no-migration policy in project docs."
    - "Release history includes an entry for the Phase 2 persistence cutover."
  artifacts:
    - path: src/routes/+layout.svelte
      provides: "Top-level one-time local reset notice UI"
      contains: "local question sets may have been reset"
    - path: README.md
      provides: "Developer-facing upgrade/wipe policy documentation"
      contains: "IndexedDB wipe"
    - path: CHANGELOG.md
      provides: "Release-note traceability for schema wipe behavior"
      contains: "02-indexeddb-persistence"
  key_links:
    - from: "src/routes/+layout.ts"
      to: "src/routes/+layout.svelte"
      via: "UI flag/load data controlling notice visibility"
      pattern: "wipe|notice|upgrade"
    - from: "README.md"
      to: "CHANGELOG.md"
      via: "Consistent local-reset policy wording"
      pattern: "wipe|re-import"
---

<objective>
Implement user and developer communication for accepted IndexedDB wipe behavior introduced by the schema cutover.

Purpose: Fulfill DATA-03 with explicit, testable communication instead of implicit behavior.
Output: One-time user notice in UI, README policy section, and release-note entry.
</objective>

<execution_context>
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-indexeddb-persistence/02-CONTEXT.md
@.planning/phases/02-indexeddb-persistence/02-RESEARCH.md
@src/routes/+layout.svelte
@README.md
</context>

<decision_coverage>
| Decision | Task | Coverage |
|----------|------|----------|
| D-10 | Task 1 | Full |
| D-11 | Task 2 | Full |
| D-12 | Task 2 | Full |
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App upgrade state -> user-facing messaging | Internal schema changes must be communicated to avoid misleading state assumptions. |
| Code changes -> operator/developer understanding | Upgrade behavior must be discoverable in repo documentation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | R | `src/routes/+layout.svelte` | mitigate | Show explicit post-upgrade notice so local data reset is visible and auditable to users. |
| T-02-08 | T | `README.md` | mitigate | Document no-migration policy and re-import expectation to prevent accidental unsupported migration assumptions. |
| T-02-09 | I | `CHANGELOG.md` | accept | Release note contains no secrets; exposure risk is negligible for ASVS L1 local app context. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add one-time local reset notice in app shell (D-10)</name>
  <read_first>
    - `src/routes/+layout.svelte`
    - `src/routes/+layout.ts`
    - `src/lib/adapters/persistence/dexie/schema.ts`
  </read_first>
  <files>src/routes/+layout.ts, src/routes/+layout.svelte</files>
  <behavior>
    - After Phase 2 upgrade, users see a short notice that local sets may be reset and can be re-imported.
    - Notice can be dismissed and does not persistently block app usage.
  </behavior>
  <action>
    Implement a one-time banner/toast at app-shell level with plain-language copy: "Local question sets may have been reset after a storage upgrade. Re-import your JSON files if needed." Trigger it from a deterministic flag in layout load/client startup keyed to schema major version `3` and localStorage marker (e.g., `quizzapp-upgrade-notice-v3-seen`) so it is shown once per browser profile. Keep styling consistent with existing Tailwind usage.
  </action>
  <acceptance_criteria>
    - `rg "quizzapp-upgrade-notice-v3-seen" src/routes/+layout.ts src/routes/+layout.svelte` returns at least one match.
    - `rg "Local question sets may have been reset" src/routes/+layout.svelte` returns one match.
  </acceptance_criteria>
  <verify>
    <automated>bun run check</automated>
  </verify>
  <done>Users get an explicit upgrade reset notice once, with clear re-import guidance.</done>
</task>

<task type="auto">
  <name>Task 2: Document wipe policy and release note (D-11, D-12)</name>
  <read_first>
    - `README.md`
    - `CHANGELOG.md` (create if missing)
    - `.planning/phases/02-indexeddb-persistence/02-CONTEXT.md`
  </read_first>
  <files>README.md, CHANGELOG.md</files>
  <behavior>
    - README explains accepted Dexie wipe/no-migration policy and re-import expectation.
    - CHANGELOG includes Phase 2 entry for traceability.
  </behavior>
  <action>
    Add a README section titled "Local persistence upgrade behavior" stating that Dexie schema upgrades in this milestone may wipe browser IndexedDB and that chain-era rows are not migrated; users should re-import question sets if data is reset (D-11). Create `CHANGELOG.md` if absent and add an entry for Phase 2 with date and note about v3 schema cutover + local reset notice (D-12).
  </action>
  <acceptance_criteria>
    - `rg "Local persistence upgrade behavior|IndexedDB wipe|no migration" README.md` returns matches.
    - `rg "02-indexeddb-persistence|Dexie v3|re-import" CHANGELOG.md` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>bun run check && bun run test:unit -- --run</automated>
  </verify>
  <done>Upgrade/wipe behavior is explicitly documented for both users and developers, and release notes capture the change.</done>
</task>

</tasks>

<verification>
- `bun run check`
- `bun run test:unit -- --run`
</verification>

<success_criteria>
UI notice is present and one-time, README explains wipe/no-migration behavior, and CHANGELOG records the release-impacting persistence change.
</success_criteria>

<output>
After completion, create `.planning/phases/02-indexeddb-persistence/02-03-SUMMARY.md`
</output>

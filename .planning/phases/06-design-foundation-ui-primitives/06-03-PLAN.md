---
phase: 06-design-foundation-ui-primitives
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/routes/style-lab/+page.svelte
  - static/cursors/quiz-show-cursor.svg
  - docs/design-tokens.md
autonomous: true
requirements:
  - TOK-01
  - CMP-01
must_haves:
  truths:
    - "Navigating to /style-lab shows stage backdrop, question Panel, multicolor answer Buttons outside the card when the with-answers preview is active, a card-only centered layout when no answer rail (D-15), variant matrix, and UI-SPEC copy strings."
    - "Custom cursor applies only for pointer:fine; coarse pointers keep default; focus remains visible on tab through controls."
    - "Success vs wrong feedback demos differ; prefers-reduced-motion disables confetti/shake-class motion."
    - "docs/design-tokens.md documents token names, html 112.5%, 44px minimum, and /style-lab entry."
  artifacts:
    - path: "src/routes/style-lab/+page.svelte"
      provides: "Pilot composition proving TOK-01 + CMP-01"
    - path: "static/cursors/quiz-show-cursor.svg"
      provides: "Large cursor asset (D-16)"
    - path: "docs/design-tokens.md"
      provides: "Short token application guide"
  key_links:
    - from: "src/routes/style-lab/+page.svelte"
      to: "src/lib/components/Button.svelte"
      via: "import + composition"
      pattern: "from '\\$lib/components/Button.svelte'"
    - from: "src/routes/style-lab/+page.svelte"
      to: "src/routes/layout.css"
      via: "stage-quiz-show + token utilities"
      pattern: "stage-quiz-show|text-role-"
---

<objective>
Deliver the **D-11** pilot at route **`/style-lab`** ( **D-12** path locked here ) composing **stage shell**, **question `Panel`**, **multicolor answer row outside the card** (**D-15**), **Button variant matrix**, **UI-SPEC copywriting**, **large custom cursor** with **touch fallback** (**D-16**), and **success vs wrong feedback** demos with **`prefers-reduced-motion`** guards (**D-17**, **D-18**, CSS/Svelte only — **no anime.js**). Add **`docs/design-tokens.md`** explaining how to apply tokens (TOK-01) and primary sizing (CMP-01).

Purpose: Prove primitives without touching funnel/run routes; give executors a visual contract reference.

Output: `+page.svelte`, cursor SVG, short doc; `bun run build` + `bun run check` + client tests pass.
</objective>

<execution_context>
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-design-foundation-ui-primitives/06-UI-SPEC.md
@.planning/phases/06-design-foundation-ui-primitives/06-CONTEXT.md
@src/lib/components/Button.svelte
@src/lib/components/Panel.svelte
@src/routes/layout.css
</context>

<tasks>

<task id="06-03-01">
  <name>Task 06-03-01: /style-lab route — stage, panel, buzzer row, variant matrix, copy</name>
  <type>implementation</type>
  <files>src/routes/style-lab/+page.svelte</files>
  <read_first>
    src/lib/components/Button.svelte
    src/lib/components/Panel.svelte
    src/routes/layout.css
    .planning/phases/06-design-foundation-ui-primitives/06-UI-SPEC.md
  </read_first>
  <action>
    Create `src/routes/style-lab/+page.svelte` (SvelteKit route **`/style-lab`**).

    **Layout (06-UI-SPEC focal hierarchy):**
    - Outermost wrapper: `class="stage-quiz-show min-h-screen"` + inner `max-w-*` column with vertical spacing using spacing tokens (`gap-xl`, `p-lg`, etc.).
    - **Primary focal:** a large **preview canvas** region (e.g. `aria-label="Design preview canvas"` on a `section`) with **local state** `answerLayout: 'withAnswers' | 'cardOnly'` (or equivalent names), toggled by **two** controls:
      - **"Show answer row"** — sets `answerLayout` to **`withAnswers`**.
      - **"Card only (no choices)"** — sets `answerLayout` to **`cardOnly`** (**D-15** second clause: when a step has no answer buttons, card is the focal element with **no** empty button rail).

      **When `withAnswers`:** the canvas contains:
      - One **`Panel`** `variant="question"` with heading `text-role-title` (**"Sample question card"**) and body `text-role-body`.
      - **Below or beside the card (not inside `Panel` children for answers):** a **row** of **four** `<Button>` for **A–D** with **`bg-answer-a`** … **`bg-answer-d`** and `text-white` where contrast passes.

      **When `cardOnly`:** render **only** the **`Panel`** `variant="question"` **centered** in the canvas (flex/grid centering); **do not** render the answer row; include visible helper text **exactly:** **"No answer buttons — card-centered layout (D-15)."**
    - **Secondary focal:** a **primitive picker** — e.g. a list of buttons or tabs that set local state `selectedPreview: 'buttons' | 'card' | 'typography'` (or similar) controlling what the canvas emphasizes; when nothing selected, show **empty state** copy **exactly**:
      - Heading: **"No sample selected"**
      - Body: **"Pick a primitive in the list to preview tokens and states."**
    - **Tertiary:** page title using `text-role-display`: **"Style lab"** (or **"Quiz show style lab"**).
    - **Primary CTA** copy (06-UI-SPEC): include a **`Button` `variant="primary"`** with label **"Open design preview"** that scrolls or sets state to show the main canvas (implement minimal behavior: e.g. sets `selectedPreview` to `'card'`).

    **Variant matrix:** Render a grid showing **all four** `Button` variants (`primary`, `secondary`, `tertiary`, `destructive`) with labels **Primary**, **Secondary**, **Tertiary**, **Destructive**.

    **Error / destructive copy (static demo blocks — optional separate small section):** Surface the exact strings from 06-UI-SPEC **Copywriting Contract** for **Error state** and **Destructive confirmation** as **non-interactive** text blocks or disabled examples:
    - Error: **"Something went wrong loading the preview."** and **"Refresh the page or return to the home screen."**
    - Destructive confirmation: **"Reset preview"** / **"Reset local preview state? This only affects the lab page."**

    Do **not** import SessionEngine or touch `/sessions/*` routes.
  </action>
  <acceptance_criteria>
    - `test -f src/routes/style-lab/+page.svelte` exits 0.
    - `rg -F "Open design preview" src/routes/style-lab/+page.svelte` exits 0.
    - `rg -F "No sample selected" src/routes/style-lab/+page.svelte` exits 0.
    - `rg -F "Pick a primitive in the list to preview tokens and states." src/routes/style-lab/+page.svelte` exits 0.
    - `rg "bg-answer-a" src/routes/style-lab/+page.svelte` exits 0.
    - `rg "bg-answer-b" src/routes/style-lab/+page.svelte` exits 0.
    - `rg "bg-answer-c" src/routes/style-lab/+page.svelte` exits 0.
    - `rg "bg-answer-d" src/routes/style-lab/+page.svelte` exits 0.
    - `rg -F "No answer buttons — card-centered layout (D-15)." src/routes/style-lab/+page.svelte` exits 0.
    - `rg -F "Card only (no choices)" src/routes/style-lab/+page.svelte` exits 0.
    - `rg -F "Show answer row" src/routes/style-lab/+page.svelte` exits 0.
    - `rg "variant=\"question\"" src/routes/style-lab/+page.svelte` exits 0.
    - `rg "stage-quiz-show" src/routes/style-lab/+page.svelte` exits 0.
    - `bun run check` exits 0.
    - `bun run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run check && bun run build</automated>
  </verify>
  <done>Style-lab page composes tokens, Panel, Buttons, answer row outside card, and spec copy.</done>
</task>

<task id="06-03-02">
  <name>Task 06-03-02: D-16 cursor asset + D-17/D-18 feedback demos (CSS-only)</name>
  <type>implementation</type>
  <files>src/routes/style-lab/+page.svelte, static/cursors/quiz-show-cursor.svg</files>
  <read_first>
    src/routes/style-lab/+page.svelte
    .planning/phases/06-design-foundation-ui-primitives/06-UI-SPEC.md
    .planning/phases/06-design-foundation-ui-primitives/06-CONTEXT.md
  </read_first>
  <action>
    1. **D-16 cursor:** Add `static/cursors/quiz-show-cursor.svg` — a **simple** 48×48 (or 64×64) **SVG** “show” pointer (circle + wedge or crosshair); **hotspot** documented in CSS `cursor: url(...) X Y`.

       In `+page.svelte` `<style>`: apply custom cursor **only** inside a wrapper class (e.g. `.style-lab-cursor-zone`) using:
       ```css
       @media (pointer: fine) {
         .style-lab-cursor-zone { cursor: url('/cursors/quiz-show-cursor.svg') 16 16, auto; }
       }
       ```
       **Do not** apply custom cursor when `pointer: coarse` (omit rule outside `pointer: fine`). Ensure **keyboard** users still see **focus rings** on interactive elements (no CSS that removes `outline` without `focus-visible` replacement).

    2. **D-17 / D-18 demos:** Add two controls in the lab (e.g. secondary buttons) **"Simulate correct"** and **"Simulate wrong"** that toggle local state to show:
       - **Correct:** celebratory **CSS-only** burst (e.g. absolutely positioned `.confetti` spans with `@keyframes` transform/opacity — no `anime.js`).
       - **Wrong:** **shake** animation on the **question** `Panel` wrapper (e.g. class `animate-shake-wrong` with `@keyframes` horizontal translation).

       Under **`@media (prefers-reduced-motion: reduce)`**:
       - **Disable** confetti motion (hide layer or `animation: none`).
       - **Disable** shake; substitute **instant** `ring-2` / background flash / static icon (pick one minimal approach documented in file comment).

    Keep all keyframes and media queries **in the same** `+page.svelte` `<style>` block or a co-located approach — no new global CSS file required.
  </action>
  <acceptance_criteria>
    - `test -f static/cursors/quiz-show-cursor.svg` exits 0.
    - `rg "quiz-show-cursor.svg" src/routes/style-lab/+page.svelte` exits 0.
    - `rg "pointer: fine" src/routes/style-lab/+page.svelte` exits 0.
    - `rg "Simulate correct" src/routes/style-lab/+page.svelte` exits 0.
    - `rg "Simulate wrong" src/routes/style-lab/+page.svelte` exits 0.
    - `rg "prefers-reduced-motion" src/routes/style-lab/+page.svelte` exits 0.
    - `rg "@keyframes" src/routes/style-lab/+page.svelte` exits 0.
    - `bun run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Cursor + motion demos meet D-16/D-17/D-18 with reduced-motion fallbacks; SVG asset served from /static.</done>
</task>

<task id="06-03-03">
  <name>Task 06-03-03: docs/design-tokens.md — application guide</name>
  <type>docs</type>
  <files>docs/design-tokens.md</files>
  <read_first>
    src/routes/layout.css
    .planning/phases/06-design-foundation-ui-primitives/06-UI-SPEC.md
    .planning/phases/06-design-foundation-ui-primitives/06-CONTEXT.md
  </read_first>
  <action>
    Create **`docs/design-tokens.md`** (new file). Must include **plain-language** sections:

    1. **Source of truth:** `src/routes/layout.css` + Tailwind v4 `@theme`.
    2. **Requirement traceability:** Explicit bullets for **TOK-01** (tokens → utilities) and **CMP-01** (primary **44px** minimum, semibold, variant distinction) referencing `Button.svelte`.
    3. **Token inventory table or list:** every **`--color-*`** semantic from Plan 01 (stage, stage-foreground, surface-elevated, card, card-foreground, primary, destructive, answer-a..d[, answer-n]) and how to use as utilities (`bg-stage`, `text-stage-foreground`, etc. — match **actual** generated names from implementation).
    4. **Typography roles:** document the four `text-role-*` utilities and **Nunito** vs system stacks (**D-04**).
    5. **Root scale:** **112.5%** on `html` (**D-14**) and consequence for rem spacing.
    6. **Style lab:** state route **`/style-lab`**, purpose (**D-11**), and that it is safe to iterate without affecting teacher flows.
    7. **Reduced motion / cursor:** summarize **D-16**, **D-13**, **D-18** expectations for future phases.

    Keep under **~120 lines**; no SessionEngine discussion.
  </action>
  <acceptance_criteria>
    - `test -f docs/design-tokens.md` exits 0.
    - `rg "TOK-01" docs/design-tokens.md` exits 0.
    - `rg "CMP-01" docs/design-tokens.md` exits 0.
    - `rg "/style-lab" docs/design-tokens.md` exits 0.
    - `rg "112.5" docs/design-tokens.md` exits 0.
    - `rg "44" docs/design-tokens.md` exits 0.
    - `rg "layout.css" docs/design-tokens.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/design-tokens.md && rg -q "TOK-01" docs/design-tokens.md && rg -q "CMP-01" docs/design-tokens.md && rg -q "/style-lab" docs/design-tokens.md</automated>
  </verify>
  <done>Short token doc exists with traceability, inventory, and style-lab pointer.</done>
</task>

</tasks>

<verification>
Run `bun run test:unit -- --run --project client src/lib/components/Button.svelte.spec.ts src/lib/components/Panel.svelte.spec.ts` (regression), `bun run check`, `bun run build`. Manual smoke: open `http://localhost:5173/style-lab` during dev (not automated here).
</verification>

<success_criteria>
- Pilot route `/style-lab` demonstrates TOK-01 utilities and CMP-01 buttons/panels together.
- D-15: Answer colors outside white card.
- D-16–D-18: Cursor + motion behaviors documented and gated by media queries.
</success_criteria>

<must_haves>
<truths>
  <truth>/style-lab exists and imports Button + Panel.</truth>
  <truth>Answer row uses bg-answer-a through bg-answer-d outside question Panel.</truth>
  <truth>docs/design-tokens.md references TOK-01, CMP-01, and /style-lab.</truth>
</truths>
<artifacts>
  <artifact path="src/routes/style-lab/+page.svelte" />
  <artifact path="static/cursors/quiz-show-cursor.svg" />
  <artifact path="docs/design-tokens.md" />
</artifacts>
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lab page → teacher trust | Lab demos must not train misleading destructive/primary patterns. |
| Motion / cursor → user | ASVS L1 presentation safety: avoid vestibular harm; preserve keyboard focus. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Spoofing | Destructive demo copy | mitigate | Use spec **Reset preview** wording that clarifies **lab-only** scope; do not label destructive button as primary CTA. |
| T-06-11 | Denial of service | Confetti / shake | mitigate | `prefers-reduced-motion: reduce` disables particle motion and shake; substitute static state (**D-18**). |
| T-06-12 | Tampering | Custom cursor blocking clicks | mitigate | Hotspot `16 16` (adjust if SVG size changes) + `, auto` fallback; **no** `cursor` on `pointer: coarse` (**D-16**). |
| T-06-13 | Elevation of privilege | N/A | accept | Static lab page. |
| T-06-14 | Information disclosure | Error copy | accept | Generic message only; no stack traces. |
| T-06-15 | Repudiation | N/A | accept | No audit claims. |
| T-06-16 | Information disclosure (focus) | focus outline removed | mitigate | Do not remove `focus-visible` rings on lab controls; tab order must remain logical. |
</threat_model>

<output>
After completion, create `.planning/phases/06-design-foundation-ui-primitives/06-03-SUMMARY.md`
</output>

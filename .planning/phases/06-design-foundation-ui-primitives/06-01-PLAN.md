---
phase: 06-design-foundation-ui-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/layout.css
autonomous: true
requirements:
  - TOK-01
must_haves:
  truths:
    - "layout.css defines @theme tokens that map to Tailwind utilities for stage, surfaces, primary/destructive, four answer slots, spacing scale, radii, shadows, and display font family."
    - "Four typography role utilities match 06-UI-SPEC sizes, weights (400/600 only), and line heights; display/title use the webfont stack, body/label use system UI."
    - "html uses font-size 112.5%; stage uses CSS animated grid (or equivalent) with a prefers-reduced-motion static/minimal fallback."
  artifacts:
    - path: "src/routes/layout.css"
      provides: "@theme tokens, @font-face or documented font import, @utility roles, base html scale, stage motion + reduced-motion branch"
  key_links:
    - from: "src/routes/layout.css"
      to: "downstream utilities (bg-stage, text-role-display, etc.)"
      via: "@theme and @utility"
      pattern: "@theme|@utility text-role-"
---

<objective>
Establish the **TOK-01** design-token layer in `src/routes/layout.css`: semantic colors (including per-answer buzzer slots), spacing scale, elevation (radius + shadow), display webfont + four **typography roles**, global **112.5%** root scale (**D-14**), and a **CSS-only** animated stage motif with **`prefers-reduced-motion`** fallback (**D-13**).

Purpose: Funnel/run phases consume one token source; no per-route hex soup.

Output: Extended `layout.css` compiling under Tailwind v4; `bun run build` proves tokens resolve.
</objective>

<execution_context>
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-design-foundation-ui-primitives/06-CONTEXT.md
@.planning/phases/06-design-foundation-ui-primitives/06-RESEARCH.md
@.planning/phases/06-design-foundation-ui-primitives/06-UI-SPEC.md
@src/routes/layout.css
</context>

<tasks>

<task id="06-01-01">
  <name>Task 06-01-01: @theme semantic colors + answer slots + spacing/radius/shadow + font-display</name>
  <type>implementation</type>
  <files>src/routes/layout.css</files>
  <read_first>
    src/routes/layout.css
    .planning/phases/06-design-foundation-ui-primitives/06-UI-SPEC.md
    .planning/phases/06-design-foundation-ui-primitives/06-CONTEXT.md
    .planning/phases/06-design-foundation-ui-primitives/06-RESEARCH.md
  </read_first>
  <action>
    In `src/routes/layout.css`, after existing `@import` / `@plugin` lines, add a top-level `@theme { ... }` block (Tailwind v4) that defines **at minimum** these **CSS variables** so Tailwind emits matching utilities (TOK-01, **D-01–D-03**, UI-SPEC § Color / Spacing):

    **Colors (use example hex from 06-UI-SPEC unless tuning while keeping contrast intent):**
    - `--color-stage: #0c0c12`
    - `--color-stage-foreground: #f4f4f5` (light chrome on stage, **D-02**)
    - `--color-surface-elevated: #f7f7f8`
    - `--color-card: #ffffff`
    - `--color-card-foreground: #18181b` (dark type inside cards, **D-03**)
    - `--color-primary: #e11d48` (accent CTA / focus emphasis per spec)
    - `--color-destructive: #dc2626` (**D-08**)
    - `--color-answer-a: #2563eb`, `--color-answer-b: #16a34a`, `--color-answer-c: #ca8a04`, `--color-answer-d: #9333ea` (distinct buzzer hues, **D-15**)
    - Optionally `--color-answer-n` for E+ cycling (document in token doc phase if added).

    **Spacing scale (UI-SPEC names → px intent at default root; express with rem):** `--spacing-xs` 4px, `sm` 8px, `md` 16px, `lg` 24px, `xl` 32px, `2xl` 48px, `3xl` 64px (use `calc()`/rem values consistent with spec table).

    **Radius:** at least `--radius-panel` (large, for floating panels/cards, **D-10**) and `--radius-control` (smaller, for secondary/tertiary geometry **D-07**).

    **Shadow:** at least one `--shadow-panel` (noticeable elevation for panels/cards).

    **Font family token:** `--font-display` set to a **rounded, friendly** webfont stack with **Nunito** as the first named family (UI-SPEC example; **D-04–D-06**) — final files loaded in task 06-01-02 via `@font-face` or equivalent; here only declare the family name in `--font-display`.

    Do **not** nest `@theme` inside selectors. Keep existing `@import 'tailwindcss'` and plugins unchanged.
  </action>
  <acceptance_criteria>
    - `rg -F "--color-stage:" src/routes/layout.css` exits 0.
    - `rg -F "--color-answer-a:" src/routes/layout.css` exits 0.
    - `rg -F "--font-display:" src/routes/layout.css` exits 0.
    - `rg -F "@theme" src/routes/layout.css` exits 0.
    - `bun run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>@theme exposes stage, card, primary, destructive, four answer colors, spacing, radius, shadow, and display font token per 06-UI-SPEC; build passes.</done>
</task>

<task id="06-01-02">
  <name>Task 06-01-02: Webfont delivery + html 112.5% + four @utility typography roles</name>
  <type>implementation</type>
  <files>src/routes/layout.css</files>
  <read_first>
    src/routes/layout.css
    .planning/phases/06-design-foundation-ui-primitives/06-UI-SPEC.md
    .planning/phases/06-design-foundation-ui-primitives/06-CONTEXT.md
  </read_first>
  <action>
    In `src/routes/layout.css`:

    1. **D-14:** Add `@layer base { html { font-size: 112.5%; } }` (or equivalent valid Tailwind v4 layering) so global rem-based utilities scale up.

    2. **D-04–D-06:** Load **Nunito** weights **400** and **600** only (UI-SPEC allows exactly these weights). Prefer **self-hosted** `static/fonts/nunito-latin-400.woff2` and `static/fonts/nunito-latin-600.woff2` with `@font-face { font-family: 'Nunito'; font-display: swap; ... }` **or** a documented subset under `static/fonts/`; if self-hosting is infeasible in one commit, use a **single** `link`-free `@import` from `fonts.googleapis.com` for Nunito 400+600 only and note bundle/perf tradeoff in `docs/design-tokens.md` (Plan 03 will reference).

    3. Add **four** Tailwind v4 **`@utility`** rules with **exact** typography from 06-UI-SPEC § Typography table:
    - `text-role-display`: weight **600**, line-height **1.2**, font-size **`clamp(2rem, 4vw, 2.75rem)`**, `font-family: var(--font-display)`
    - `text-role-title`: **1.5rem**, weight **600**, line-height **1.2**, `font-family: var(--font-display)`
    - `text-role-body`: **1rem**, weight **400**, line-height **1.5**, **system UI** stack (`ui-sans-serif`, `system-ui`, `sans-serif`) — not Nunito
    - `text-role-label`: **0.875rem**, weight **600**, line-height **1.5**, system UI stack

    No other font weights in these utilities.
  </action>
  <acceptance_criteria>
    - `rg -F "font-size: 112.5%" src/routes/layout.css` exits 0.
    - `rg "@utility text-role-display" src/routes/layout.css` exits 0.
    - `rg "@utility text-role-title" src/routes/layout.css` exits 0.
    - `rg "@utility text-role-body" src/routes/layout.css` exits 0.
    - `rg "@utility text-role-label" src/routes/layout.css` exits 0.
    - `rg "Nunito" src/routes/layout.css` exits 0.
    - `bun run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Root scale, Nunito 400/600 for display/title roles, system stack for body/label, and four role utilities match 06-UI-SPEC numerics.</done>
</task>

<task id="06-01-03">
  <name>Task 06-01-03: CSS stage backdrop motion + prefers-reduced-motion fallback</name>
  <type>implementation</type>
  <files>src/routes/layout.css</files>
  <read_first>
    src/routes/layout.css
    .planning/phases/06-design-foundation-ui-primitives/06-UI-SPEC.md
    .planning/phases/06-design-foundation-ui-primitives/06-CONTEXT.md
  </read_first>
  <action>
    In `src/routes/layout.css`, add a **reusable** stage backdrop pattern for the style-lab (and later funnel), **D-13**:

    - Define a class e.g. `.stage-quiz-show` on a full-viewport wrapper: `background-color: var(--color-stage)` (or `bg-stage` if applied via `@apply` in a layer — prefer token `var(--color-stage)` in raw CSS for clarity).
    - Add a **pseudo-element** (e.g. `::before`) that draws a **subtle grid or line motif** using CSS gradients/lines, animated with **`transform` + `@keyframes`** (looping slide — pure CSS, no JS).
    - Under **`@media (prefers-reduced-motion: reduce)`**, **disable** the busy looping motion: use a **static** background or a **single subtle opacity pulse** only (no continuous grid drift), per **D-13** / **D-18**.

    Do not introduce **anime.js** in this plan.
  </action>
  <acceptance_criteria>
    - `rg "stage-quiz-show" src/routes/layout.css` exits 0.
    - `rg "prefers-reduced-motion" src/routes/layout.css` exits 0.
    - `rg "@keyframes" src/routes/layout.css` exits 0.
    - `bun run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <done>Stage class provides animated motif with reduced-motion-safe fallback; no JS animation dependency.</done>
</task>

</tasks>

<verification>
Run `bun run build` after all tasks; optional `bun run check` if TypeScript-adjacent edits are avoided (should be none). Spot-check in DevTools that `@theme` colors appear as utilities in generated CSS (build output).
</verification>

<success_criteria>
- TOK-01: Token-backed utilities exist for stage, surfaces, accents, answers, spacing, elevation, and typography roles.
- D-13/D-18: Stage motion respects `prefers-reduced-motion`.
- D-14: `html` font-size 112.5% applied globally.
</success_criteria>

<must_haves>
<truths>
  <truth>layout.css contains @theme with --color-stage, --color-card, --color-primary, --color-destructive, and --color-answer-a through --color-answer-d.</truth>
  <truth>Four @utility text-role-* classes match 06-UI-SPEC numeric table.</truth>
  <truth>.stage-quiz-show provides CSS motion with prefers-reduced-motion fallback.</truth>
</truths>
<artifacts>
  <artifact path="src/routes/layout.css" />
</artifacts>
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS → user agent | Malicious CSS injection is out of scope; file is author-controlled. |
| Motion → user | Vestibular / seizure sensitivity via excessive motion (**D-13**/**D-18**). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of service (sensory) | `.stage-quiz-show` animation | mitigate | `prefers-reduced-motion: reduce` removes busy looping motion; static or minimal fallback only. |
| T-06-02 | Tampering (misleading chrome) | High-contrast tokens | mitigate | Preserve spec intent: light text on stage (`--color-stage-foreground`), dark text on card (`--color-card-foreground`) so labels are not camouflaged. |
| T-06-03 | Information disclosure | N/A (no PII in CSS) | accept | No sensitive data in theme file. |
</threat_model>

<output>
After completion, create `.planning/phases/06-design-foundation-ui-primitives/06-01-SUMMARY.md`
</output>

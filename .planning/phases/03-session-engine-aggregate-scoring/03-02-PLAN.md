---
phase: 03-session-engine-aggregate-scoring
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/routes/sessions/[id]/run/+page.svelte
  - src/routes/sessions/[id]/run/+page.ts
  - src/lib/domain/session-engine/session-engine.test.ts
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-04
must_haves:
  truths:
    - "Run UI renders engine-owned current step and k-of-n progression without duplicating authority."
    - "Pause/reload resumes the same logical unit and step in run flow."
    - "Skipped in-progress logical units produce no Attempt and are not redrawn in the same session."
  artifacts:
    - path: "src/routes/sessions/[id]/run/+page.svelte"
      provides: "Engine-authoritative run rendering and controls"
    - path: "src/routes/sessions/[id]/run/+page.ts"
      provides: "Route load with session state consumed by engine resume logic"
  key_links:
    - from: "src/routes/sessions/[id]/run/+page.svelte"
      to: "src/lib/domain/session-engine/index.ts"
      via: "currentStep/currentStepIndex/totalSteps getters"
      pattern: "engine\\.(currentStep|currentStepIndex|totalSteps)"
    - from: "src/routes/sessions/[id]/run/+page.svelte"
      to: "src/lib/domain/session-engine/index.ts"
      via: "skip action calling engine skip method"
      pattern: "skip"
---

<objective>
Wire the run route to SessionEngine’s new authoritative progression contract and complete skip/resume flow consistency.

Purpose: Ensure the live session UI never becomes a competing source of truth and preserves one-attempt-per-completed-unit semantics.
Output: Updated run route behavior plus integration-focused engine test coverage for skip and resume flow.
</objective>

<execution_context>
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-session-engine-aggregate-scoring/03-CONTEXT.md
@.planning/phases/03-session-engine-aggregate-scoring/03-01-PLAN.md
@src/routes/sessions/[id]/run/+page.ts
@src/routes/sessions/[id]/run/+page.svelte
@src/lib/domain/session-engine/index.ts
@src/lib/domain/session-engine/session-engine.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bind run page progression to engine authority only</name>
  <read_first>
    src/routes/sessions/[id]/run/+page.svelte
    src/lib/domain/session-engine/index.ts
    .planning/phases/03-session-engine-aggregate-scoring/03-CONTEXT.md
  </read_first>
  <files>
    src/routes/sessions/[id]/run/+page.svelte
  </files>
  <action>
    Replace step rendering that infers from `currentQuestion.steps[0]` and `chainProgress` with engine getters from Plan 03-01:
    `engine.currentStep`, `engine.currentStepIndex`, and `engine.totalSteps`.
    Render progression text exactly as `Step {engine.currentStepIndex + 1} of {engine.totalSteps}`.
    Keep student/question slot progress header, but remove chain-era label text `Chain question`.
    Add a `Skip Unit` control that calls the new engine skip method and advances to next unit without creating an Attempt (D-03, D-04).
    Ensure keyboard shortcuts still apply only to open-answer steps; MC/TF continue click-only grading.
  </action>
  <acceptance_criteria>
    - `rg "engine\\.currentStep|engine\\.currentStepIndex|engine\\.totalSteps" src/routes/sessions/[id]/run/+page.svelte` returns all three references.
    - `rg "Chain question" src/routes/sessions/[id]/run/+page.svelte` returns no matches.
    - `rg "Skip Unit|skip" src/routes/sessions/[id]/run/+page.svelte` shows skip UI and handler wiring.
  </acceptance_criteria>
  <verify>
    <automated>bun run check</automated>
  </verify>
  <done>Run UI displays and controls progression strictly from SessionEngine state and supports skip semantics without duplicate progression state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add resume + skip integration assertions in engine test suite</name>
  <read_first>
    src/lib/domain/session-engine/session-engine.test.ts
    src/lib/domain/session-engine/index.ts
    src/routes/sessions/[id]/run/+page.ts
    .planning/phases/03-session-engine-aggregate-scoring/03-CONTEXT.md
  </read_first>
  <files>
    src/lib/domain/session-engine/session-engine.test.ts
    src/routes/sessions/[id]/run/+page.ts
  </files>
  <action>
    Extend `session-engine.test.ts` with integration-oriented tests that assert:
    (a) persisted `active_unit_progress` reconstructs same logical root and step index after creating a fresh engine instance;
    (b) skip mid-unit produces zero `createAttempt` calls and removes current root from same-session candidate pool;
    (c) after skip, next selected unit is not the skipped root id during that session lifecycle.
    In `+page.ts`, preserve return payload fields required by resume behavior (`session`, `sessionStudents`, `students`, `allQuestions`, `attempts`) and do not transform `session.active_unit_progress`.
  </action>
  <acceptance_criteria>
    - `rg "active_unit_progress" src/lib/domain/session-engine/session-engine.test.ts src/routes/sessions/[id]/run/+page.ts` shows state passes from loader to engine setup path.
    - `rg "skip.*no Attempt|no Attempt.*skip|skipped" src/lib/domain/session-engine/session-engine.test.ts` finds explicit skip/no-attempt assertions.
    - `rg "createAttemptCalls\\).*toHaveLength\\(0\\)|not\\.toContain\\(" src/lib/domain/session-engine/session-engine.test.ts` confirms no-attempt and not-redrawn checks.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:unit -- --run src/lib/domain/session-engine/session-engine.test.ts</automated>
  </verify>
  <done>Resume and skip semantics are covered by automated tests and run-route load data stays compatible with engine-owned state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route load -> run page engine construction | Persisted session state is interpreted into active gameplay state |
| Teacher UI actions -> engine methods | Runtime controls can alter persistence-affecting state transitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | run page progression rendering | mitigate | Render progression from engine getters only; remove derived chain-era step authority in UI |
| T-03-05 | R | skip workflow and attempt audit trail | mitigate | Enforce no-attempt-on-skip and assert with tests; only completed units call createAttempt |
| T-03-06 | D | resume state parsing in route/engine | mitigate | Keep loader payload unmodified and rely on typed `active_unit_progress`; guard null/invalid state in engine constructor |
</threat_model>

<verification>
- `bun run check`
- `bun run test:unit -- --run src/lib/domain/session-engine/session-engine.test.ts`
</verification>

<success_criteria>
- Run UI shows engine-authoritative step progression (`currentStepIndex`/`totalSteps`) and no chain-era counter.
- Resume path restores current logical unit and current step after pause/reload.
- Skip path records no Attempt and does not recycle skipped logical unit in same session.
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-engine-aggregate-scoring/03-02-SUMMARY.md`
</output>

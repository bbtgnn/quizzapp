---
phase: 01-domain-model-import-contract
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/lib/importer/index.ts
  - src/lib/importer/logical-to-parsed-question-set.ts
  - src/lib/application/question-sets/persist-question-set.test.ts
autonomous: true
requirements:
  - IMPT-01
must_haves:
  truths:
    - "parseQuestionSetFile returns ParseResult; invalid JSON never yields ok:true"
    - "Valid new-format files map to ParsedQuestionSet compatible with persistQuestionSet"
    - "Logical steps[] map to root question + chain[] until Phase 2 (persistence bridge documented in code)"
  artifacts:
    - path: src/lib/importer/logical-to-parsed-question-set.ts
      provides: "logical QuestionSetFile → ParsedQuestionSet adapter"
      contains: "steps"
    - path: src/lib/importer/index.ts
      provides: "parseQuestionSetFile uses Zod safeParse + bridge"
      contains: "safeParse|ParseResult"
  key_links:
    - from: "parseQuestionSetFile"
      to: "logical-to-parsed-question-set.ts"
      via: "validated.data passed to adapter"
      pattern: "toParsedQuestionSet|logicalToParsed"
    - from: "import/+page.svelte"
      to: "parseQuestionSetFile"
      via: "unchanged call order parse then persist"
      pattern: "parseQuestionSetFile\\("
---

<objective>
Replace hand-rolled parsing with **Zod-validated** import and a **documented persistence bridge**: new-format logical questions (`shared` + `steps[]`) convert to existing `ParsedQuestionSet` / `ParsedQuestion` / `chain` so `persistQuestionSet` and the import UI keep working until Phase 2 (DATA-02).

Purpose: IMPT-01 — validation at boundary; preserves teacher workflow in `src/routes/question-sets/import/+page.svelte`.

Output: New adapter module, updated `parseQuestionSetFile`, tests proving reject paths and bridge behavior.
</objective>

<execution_context>
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-domain-model-import-contract/01-CONTEXT.md
@src/lib/application/question-sets/persist-question-set.ts
@src/routes/question-sets/import/+page.svelte

<interfaces>
From `src/lib/importer/index.ts` (preserve export names for consumers):

```typescript
export interface ParsedChainItem {
	text: string;
	answer: AnswerConfig;
}
export interface ParsedQuestion {
	text: string;
	content: ContentConfig;
	answer: AnswerConfig;
	difficulty?: string;
	chain?: ParsedChainItem[];
}
export interface ParsedQuestionSet {
	name: string;
	questions: ParsedQuestion[];
}
export type ParseResult = { ok: true; data: ParsedQuestionSet } | { ok: false; error: string };
export function parseQuestionSetFile(jsonString: string): ParseResult;
```

From `src/lib/application/question-sets/persist-question-set.ts`:

```typescript
export async function persistQuestionSet(
	questionSets: QuestionSetRepository,
	questionSetId: string,
	parsed: ParsedQuestionSet
): Promise<void>;
```

**Persistence bridge (Phase 1 → Phase 2):** For each logical question, set `ParsedQuestion.text` = `steps[0].text`, `ParsedQuestion.answer` = `steps[0].answer`, `ParsedQuestion.content` = **persistable** `ContentConfig` derived from `shared?.content` or per-step fallback specified below. For `i > 0`, `ParsedChainItem` = `{ text: steps[i].text, answer: steps[i].answer }`. If `shared` is absent, define explicit rules in code comments (e.g. default markdown body from first step only — **planner requirement:** use `steps[0].text` as `markdown` body only if product requires; **preferred:** require `shared` when multiple steps exist, or use minimal `markdown` placeholder — **lock:** implement the minimal behavior that matches migrated bundled sets: migrated files always include `shared`).

For **shared `code-snippet`**: build `ContentConfig` for **each** persisted row (root and each chain item) as `{ type: 'code-snippet', language, code, highlight: { startLine, endLine } }` where `highlight` is copied from that step’s `range` (D-05 bridge). For **shared `markdown`**: `content` is `{ type: 'markdown', body: shared.content.body }` on every row (no per-step range).

**Legacy importer JSON** (`text`/`content`/`answer`/`chain` at question root): **reject** at boundary per D-02 / project decision “one on-disk format after migration” — `parseQuestionSetFile` accepts **only** the new envelope. Bundled assets are migrated in Plan 03; tests use new shape only.
</interfaces>
</context>

<decision_coverage>
| Decision | Task | Notes |
|----------|------|-------|
| D-02 | Task 2 | Parser accepts only new format; no `chain` in JSON |
| D-08 | Task 2 | Use `formatQuestionSetParseError` from Plan 01 on Zod failure |
| D-08 | Task 3 | Tests assert error string matches `/questions\[\d+\]/` or similar path fragment |
| (bridge) | Task 1 | Document in file header + function JSDoc: steps[] → root + chain; highlight from range |
| IMPT-01 | Task 2–3 | safeParse + no persist on false (caller already guards) |
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON string → `parseQuestionSetFile` | Untrusted input; Zod + bridge must not produce invalid `AnswerConfig` / `ContentConfig` for Dexie layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `logical-to-parsed-question-set.ts` | mitigate | Only construct `ContentConfig`/`AnswerConfig` from validated Zod output shapes; no ad-hoc stringly types. |
| T-02-02 | I | `parseQuestionSetFile` | mitigate | On `safeParse` failure return `ok: false` with formatted error; never return partial `data` (IMPT-01 / V5). |
| T-02-03 | D | Bridge mapping | mitigate | Unit tests cover multi-step code-snippet (each row gets correct `highlight` from step `range`). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement logical → ParsedQuestionSet bridge</name>
  <read_first>
    - `src/lib/importer/question-set.schema.ts` (types exports)
    - `src/lib/model/types.ts` (`ContentConfig`, `AnswerConfig`)
    - `src/lib/application/question-sets/persist-question-set.ts`
  </read_first>
  <files>src/lib/importer/logical-to-parsed-question-set.ts</files>
  <action>
    Add `src/lib/importer/logical-to-parsed-question-set.ts` exporting a pure function, e.g. `logicalQuestionSetFileToParsed(data: QuestionSetFile): ParsedQuestionSet` (use the actual `z.infer` type name from Plan 01).

    **Mapping rules (encode in JSDoc + inline comments):**
    - `ParsedQuestionSet.name` = file `name`.
    - For each logical question: `steps[0]` becomes root `text` / `answer`; `steps.slice(1)` become `chain` items with only `text` + `answer`.
    - `content` for root and every chain row: if `shared?.content.type === 'code-snippet'`, use `{ type: 'code-snippet', language, code, highlight: { startLine: step.range.startLine, endLine: step.range.endLine } }` for **that step’s** row (root uses step 0 range, chain item *i* uses step *i+1* range). If `shared?.content.type === 'markdown'`, use same markdown `body` for all rows; **must not** attach `highlight`.
    - `difficulty`: pass through from logical question if present in schema.
    - If `shared` is missing: use `{ type: 'markdown', body: '' }` or derive single-step-only content from step text — **pick one** and document; prefer matching how migrated JSON in Plan 03 always includes `shared` for fundamentals set.

    Do **not** call `persistQuestionSet` here; adapter is pure.
  </action>
  <acceptance_criteria>
    - File exports a function imported by `index.ts` in Task 2.
    - `rg "steps\\[0\\]|chain" src/lib/importer/logical-to-parsed-question-set.ts` has matches.
    - `rg "Phase 2|DATA-02|persistence bridge" src/lib/importer/logical-to-parsed-question-set.ts` has at least one documentation match (comment).
  </acceptance_criteria>
  <verify>
    <automated>bun run check</automated>
  </verify>
  <done>Bridge function compiles and documents steps→chain mapping and highlight bridging.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire parseQuestionSetFile to Zod + bridge</name>
  <read_first>
    - `src/lib/importer/index.ts` (current implementation)
    - `src/lib/importer/errors.ts`, `src/lib/importer/question-set.schema.ts`
    - `src/lib/importer/logical-to-parsed-question-set.ts`
  </read_first>
  <files>src/lib/importer/index.ts</files>
  <behavior>
    - `parseQuestionSetFile` returns `{ ok: false, error }` for invalid JSON string (syntax).
    - Successful validation runs bridge and returns `{ ok: true, data: ParsedQuestionSet }`.
  </behavior>
  <action>
    Rewrite `parseQuestionSetFile` in `src/lib/importer/index.ts`:
    - `JSON.parse` in try/catch → existing generic invalid JSON message on catch.
    - `QuestionSetFileSchema.safeParse(parsed)`; on failure `return { ok: false, error: formatQuestionSetParseError(result.error) }`.
    - On success `return { ok: true, data: logicalQuestionSetFileToParsed(result.data) }`.
    Remove obsolete hand-rolled `parseContentConfig` / `parseAnswerConfig` helpers from this file (delete dead code).

    Keep exporting `ParsedQuestionSet`, `ParseResult`, `ParsedQuestion`, `ParsedChainItem` unchanged for downstream imports.

    Touch `src/lib/application/question-sets/persist-question-set.ts` **only if** types or imports require a one-line comment referencing the bridge — prefer **no functional change** to persistence in this phase.
  </action>
  <acceptance_criteria>
    - `rg "parseContentConfig|parseAnswerConfig" src/lib/importer/index.ts` returns no matches.
    - `rg "safeParse" src/lib/importer/index.ts` returns ≥ 1 match.
    - `rg "logicalQuestionSetFileToParsed|toParsed" src/lib/importer/index.ts` returns ≥ 1 match.
  </acceptance_criteria>
  <verify>
    <automated>bun run check</automated>
  </verify>
  <done>Importer entrypoint validates with Zod and outputs ParsedQuestionSet via bridge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite importer unit tests for new contract + persistence smoke</name>
  <read_first>
    - `src/lib/importer/index.test.ts` (full file)
    - `src/lib/application/question-sets/persist-question-set.test.ts`
  </read_first>
  <files>src/lib/importer/index.test.ts, src/lib/application/question-sets/persist-question-set.test.ts</files>
  <behavior>
    - Valid fixture: `{ "name": "…", "schemaVersion": 1, "questions": [{ "shared": { "content": { "type": "markdown", "body": "x" } }, "steps": [{ "text": "q1", "answer": { "type": "open" } }] }] }` parses `ok: true`.
    - Unknown key at root fails `ok: false` and error mentions a path substring like `name` or `(root)` per formatter.
    - Code shared + two steps: each step has `range`; expect parsed root `content.highlight` matches first range; first chain item’s implied row would get second range — assert via **ParsedQuestionSet** structure (root `chain[0]` exists, root `content` equals expected).
    - Markdown shared + step without `range` passes; adding `range` fails.
    - Open answer with `referenceAnswer` round-trips to `ParsedQuestion` / chain answers.
  </behavior>
  <action>
    Replace legacy fixtures in `src/lib/importer/index.test.ts` that used per-question `content`/`answer`/`chain` with **schemaVersion 1** `shared` + `steps` fixtures per CONTEXT.

    Add tests for D-07: extra property on a strict object causes failure (e.g. `foo` at root).

    Update or add `persist-question-set.test.ts` cases so at least one multi-step logical input (after bridge) still persists expected number of Dexie rows (root + chain length) — use existing test doubles from that file.

    Remove tests that asserted **legacy** importer-only shape acceptance if they conflict with D-02.
  </action>
  <acceptance_criteria>
    - `bun run test:unit -- --run src/lib/importer/index.test.ts` exits 0.
    - `bun run test:unit -- --run src/lib/application/question-sets/persist-question-set.test.ts` exits 0.
    - `rg "schemaVersion" src/lib/importer/index.test.ts` matches at least once.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:unit -- --run src/lib/importer/index.test.ts src/lib/application/question-sets/persist-question-set.test.ts</automated>
  </verify>
  <done>Tests document new contract; IMPT-01 behaviors covered at unit level.</done>
</task>

</tasks>

<verification>
`bun run test:unit -- --run src/lib/importer/` passes; import page unchanged aside from any type-only churn.
</verification>

<success_criteria>
Invalid files rejected with path-bearing errors; valid logical files parse and bridge into `ParsedQuestionSet` compatible with `persistQuestionSet`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-model-import-contract/01-02-SUMMARY.md`
</output>

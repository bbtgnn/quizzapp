---
phase: 01-domain-model-import-contract
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scripts/migrate-question-sets.ts
  - question-sets/**/*.json
autonomous: true
requirements:
  - IMPT-02
must_haves:
  truths:
    - "Every file under question-sets/ uses the new envelope with schemaVersion 1"
    - "Legacy snippet+correctAnswer and importer-style chain rows are represented as shared + ordered steps"
    - "Open string answers preserve teacher text in answer.referenceAnswer where applicable (D-12)"
  artifacts:
    - path: scripts/migrate-question-sets.ts
      provides: "Idempotent-ish migration runner (review via git diff)"
      contains: "snippet|schemaVersion|steps"
    - path: question-sets/js-fundamentals/
      provides: "Migrated bundled JSON assets"
      contains: '"schemaVersion": 1'
  key_links:
    - from: "scripts/migrate-question-sets.ts"
      to: "question-sets/**/*.json"
      via: "read/write JSON on disk"
      pattern: "question-sets"
---

<objective>
Migrate **all** bundled `question-sets/**/*.json` to the **single** new import contract (D-10, D-11) using a **maintainer script** so git diff reviews content.

Purpose: IMPT-02 — bundled sets load through the Plan 02 importer.

Output: Rewritten JSON files + `scripts/migrate-question-sets.ts`.
</objective>

<execution_context>
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-domain-model-import-contract/01-RESEARCH.md § Runtime state inventory
@question-sets/js-fundamentals/001-string-literal.json
@src/lib/importer/question-set.schema.ts

**Migration input shapes (D-11):**

1. **Legacy snippet format:** top-level `snippet: { language, code, highlight? }` and `questions: [{ text, correctAnswer: string, difficulty?, chain?: [...] }]`. There is **no** per-question `highlight` in sample data — use `snippet.highlight` as the default `range` for **every** step when shared content is `code-snippet`. Each legacy row becomes one **step** with `answer: { type: 'open', referenceAnswer: <correctAnswer string> }` (D-12).

2. **Importer-style (if any file matches):** `{ name, questions: [{ text, content, answer, chain?: [{text, answer}] }] }` — convert to one logical question per legacy question: `shared: { content: <strip highlight from code-snippet content> }`, `steps`: first step from root `text`/`answer`, following steps from `chain` in order; for code-snippet shared content, set each step `range` from legacy `content.highlight` if present else **full-file default** `startLine: 1, endLine: <line count of code split by \\n>` or reuse snippet highlight rule from RESEARCH “sensible defaults”.

**Top-level `name` (D-01):** derive as `"{folderBasename} / {fileStem}"` e.g. `js-fundamentals / 001-string-literal` for files under `question-sets/js-fundamentals/`.

**Ordering nested chain:** root prompt → step 0; `chain[j]` → step j+1 (D-11 / RESEARCH pitfall 3).

**Idempotency:** If file already has `schemaVersion: 1` and required fields, skip or no-op rewrite.
</context>

<decision_coverage>
| Decision | Task | Notes |
|----------|------|-------|
| D-10 | Task 1–2 | One format on disk; script + in-place write |
| D-11 | Task 1 | Explicit branches for snippet-legacy vs importer-style |
| D-12 | Task 1 | `referenceAnswer` on open steps from `correctAnswer` string |
| IMPT-02 | Task 2 | All globbed JSON files conform after run |
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer script reading/writing repo JSON | Trusted developer tool; still validate output with the same Zod schema import as runtime to catch mistakes (optional in script: `QuestionSetFileSchema.safeParse`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I | `scripts/migrate-question-sets.ts` | mitigate | After writing each file, `safeParse` with `QuestionSetFileSchema` and throw on failure so bad migrations do not land silently. |
| T-03-02 | D | Migration output | accept | Content is teacher-facing quiz data in repo; integrity ensured by validation + tests in Plan 04. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement migrate-question-sets script</name>
  <read_first>
    - `glob_file_search question-sets/**/*.json` or `ls question-sets/js-fundamentals`
    - `src/lib/importer/question-set.schema.ts` (import path from script — use dynamic import or duplicate minimal check)
    - `011-mixed-types.json` if present (nested chain sample per RESEARCH)
  </read_first>
  <files>scripts/migrate-question-sets.ts</files>
  <action>
    Create `scripts/migrate-question-sets.ts` runnable with `bun run scripts/migrate-question-sets.ts` (or add `"migrate:question-sets": "bun run scripts/migrate-question-sets.ts"` to `package.json` **only if** needed for discoverability).

    Behavior:
    - Recursively find all `question-sets/**/*.json`.
    - For each file, read JSON; detect legacy snippet format vs importer-style vs already-migrated (`schemaVersion === 1` and has `name` + `questions` with `steps`).
    - Emit new object: `{ name, schemaVersion: 1, questions: LogicalQuestion[] }` per CONTEXT.
    - Pretty-print JSON with project Prettier conventions (tabs, no trailing commas) **or** use `JSON.stringify(data, null, '\t')` matching existing bundled indent style — pick one and match neighboring files.

    At end of per-file write, validate with `QuestionSetFileSchema.safeParse` (import from `$lib` may not work in script — use relative import `../src/lib/importer/question-set.schema.ts` or compile step; **preferred:** use `bun` to run TS and import from `src/lib/importer/question-set.schema.ts` via relative path).

    Document migration rules in a top-of-file comment block (D-11 summary).
  </action>
  <acceptance_criteria>
    - `test -f scripts/migrate-question-sets.ts` is true.
    - `rg "schemaVersion" scripts/migrate-question-sets.ts` matches.
    - `rg "referenceAnswer|correctAnswer" scripts/migrate-question-sets.ts` matches (mapping documented in code).
  </acceptance_criteria>
  <verify>
    <automated>bun run scripts/migrate-question-sets.ts --help 2>/dev/null || bun run scripts/migrate-question-sets.ts</automated>
  </verify>
  <note>If script has no --help, bare run should migrate or no-op with exit 0.</note>
  <done>Script exists, documents rules, validates output shape.</done>
</task>

<task type="auto">
  <name>Task 2: Execute migration and verify JSON count</name>
  <read_first>
    - `scripts/migrate-question-sets.ts`
    - `find question-sets -name '*.json' | wc -l` baseline before/after
  </read_first>
  <files>question-sets/**/*.json</files>
  <action>
    Run the migration script against the repo so **all** `question-sets/**/*.json` are rewritten.

    Manual expectation: `git diff question-sets/` shows substantive edits reviewed by executor; no remaining top-level `snippet` keys in `question-sets/js-fundamentals/` after migration.

    If a file fails validation after transform, fix the script and re-run until all pass.
  </action>
  <acceptance_criteria>
    - `find question-sets -name '*.json' | wc -l` equals count of files where first non-whitespace after `{` includes `"name"` and `"schemaVersion": 1` (use `rg -l '"schemaVersion": 1' question-sets --glob '*.json' | wc -l` equals file count).
    - `rg '"snippet"' question-sets --glob '*.json'` returns **no** matches (legacy key removed).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'c=$(find question-sets -name "*.json" 2>/dev/null | wc -l | tr -d " "); v=$(rg -l "\"schemaVersion\"" question-sets --glob "*.json" 2>/dev/null | wc -l | tr -d " "); test "$c" -gt 0 && test "$c" -eq "$v"' && bun run test:unit -- --run src/lib/importer/index.test.ts</automated>
  </verify>
  <done>All bundled JSON files migrated; importer unit tests still green (full `question-sets/**/*.json` parse coverage is Plan 04 / D-13).</done>
</task>

</tasks>

<verification>
`rg '"snippet"' question-sets` empty; `parseQuestionSetFile` succeeds on each file (Task 3 or Plan 04).
</verification>

<success_criteria>
Bundled question sets are in the new format only; migration is repeatable from script.
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-model-import-contract/01-03-SUMMARY.md`
</output>

---
phase: 04-live-session-run-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/domain/session-engine/session-engine.test.ts
  - src/routes/sessions/[id]/run/+page.svelte
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
must_haves:
  truths:
    - "Run view shows shared stem/context when the logical unit defines `shared`, the current step prompt, and k-of-n text derived only from SessionEngine step indices."
    - "Each step uses open, multiple-choice, or true-false interactions according to that step’s `answer.type`."
    - "The run route does not introduce a separate authoritative step index that can diverge from the engine."
  artifacts:
    - path: "src/routes/sessions/[id]/run/+page.svelte"
      provides: "Live run UI bound to engine getters; requirement traceability note"
    - path: "src/lib/domain/session-engine/session-engine.test.ts"
      provides: "Automated coverage for display-relevant engine contract (multi-step + shared stem)"
  key_links:
    - from: "src/routes/sessions/[id]/run/+page.svelte"
      to: "src/lib/domain/session-engine/index.ts"
      via: "currentStep, currentStepIndex, totalSteps, currentQuestion"
      pattern: "engine\\.(currentStep|currentStepIndex|totalSteps|currentQuestion)"
---

<objective>
Close Phase 4 (Live session run UI) by **verifying and locking** UI-01–UI-03 against the current run route and SessionEngine contract, with **minimal UI change** per `04-CONTEXT.md` (D-01–D-05). **Do not** implement deferred per-step `range` / `CodeBlock` emphasis (D-02 deferred).

Output: targeted automated tests for engine/view data contract plus an explicit traceability note on the run page; `bun run check` and scoped unit tests pass.
</objective>

<execution_context>
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-live-session-run-ui/04-CONTEXT.md
@.planning/phases/03-session-engine-aggregate-scoring/03-CONTEXT.md
@src/routes/sessions/[id]/run/+page.svelte
@src/routes/sessions/[id]/run/+page.ts
@src/lib/domain/session-engine/index.ts
@src/lib/domain/session-engine/session-engine.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add engine contract tests for run UI data (UI-01 / UI-02)</name>
  <read_first>
    src/lib/domain/session-engine/session-engine.test.ts
    src/lib/domain/session-engine/index.ts
    src/lib/model/types.ts
    .planning/phases/04-live-session-run-ui/04-CONTEXT.md
  </read_first>
  <files>
    src/lib/domain/session-engine/session-engine.test.ts
  </files>
  <action>
    Add a dedicated `describe('run UI data contract', ...)` block in `session-engine.test.ts` with **Vitest** cases that construct a **SessionEngine** from in-memory fixtures (reuse existing test helpers/patterns in the file) where:
    (a) A logical `Question` has **`shared.content`** (markdown or code-snippet) and **at least two `steps`** with different **`answer.type`** values (e.g. one `open`, one `multiple-choice`). Assert `currentQuestion` (engine getter) still exposes **`shared`** from the root while **`currentStep` / `currentStepIndex` / `totalSteps`** reflect the active step, and that advancing steps changes `currentStep.answer.type` as expected (per D-05, UI-02).
    (b) Single-step unit: `totalSteps === 1`, `currentStepIndex === 0`, labels would read **Step 1 of 1** (assert numeric getters; UI-01 k-of-n).
    Do **not** add tests for per-step `range`/`CodeBlock` (deferred in 04-CONTEXT D-02).
    Reference requirement IDs in test titles or comments: UI-01, UI-02.
  </action>
  <acceptance_criteria>
    - `rg "run UI data contract|UI-01|UI-02" src/lib/domain/session-engine/session-engine.test.ts` returns matches.
    - `rg "shared" src/lib/domain/session-engine/session-engine.test.ts` appears in the new block (fixture or assertion referencing shared stem).
    - `bun run test:unit -- --run src/lib/domain/session-engine/session-engine.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:unit -- --run src/lib/domain/session-engine/session-engine.test.ts</automated>
  </verify>
  <done>Engine exposes shared stem + per-step answer types in a way the run page can render without parallel step authority (UI-01/02 backed by tests).</done>
</task>

<task type="auto">
  <name>Task 2: Run page traceability + anti-desync guardrails (UI-03)</name>
  <read_first>
    src/routes/sessions/[id]/run/+page.svelte
    src/lib/domain/session-engine/index.ts
    .planning/phases/04-live-session-run-ui/04-CONTEXT.md
  </read_first>
  <files>
    src/routes/sessions/[id]/run/+page.svelte
  </files>
  <action>
    At the top of `<script lang="ts">` in `+page.svelte`, add a **short comment block** (not user-visible) that:
    - Lists **UI-01**, **UI-02**, **UI-03** and states that **step index / total steps / current step** come **only** from `SessionEngine` (`currentStepIndex`, `totalSteps`, `currentStep`, `currentQuestion`) per D-05.
    - Explicitly says **no local authoritative `stepIndex` state** — reactive display must be derived from `engine` getters + existing `tick` refresh pattern only.
    - References **UI-02**: interaction UI is selected by `currentAnswer?.type` branches (`open` / `multiple-choice` / `true-false`) from the engine-derived `currentStep` / `currentQuestion` chain (see existing `{#if}` / `handleMCOption` / `handleTFButton` paths).
    Perform a **read-only audit**: if you find any local variable that stores step position independently of the engine, **remove it** and derive from engine only (per UI-03). If no such variable exists, **do not refactor** layout (D-01).
  </action>
  <acceptance_criteria>
    - `rg "UI-01|UI-02|UI-03" src/routes/sessions/[id]/run/+page.svelte` returns matches in the script comment block.
    - `rg "currentStepIndex|totalSteps|currentStep" src/routes/sessions/[id]/run/+page.svelte` shows these used for progression/display.
    - `rg "Step \\{currentStepIndex \\+ 1\\} of \\{totalSteps\\}" src/routes/sessions/[id]/run/+page.svelte` matches the k-of-n line **or** an equivalent concatenation using the same variables.
    - **UI-02 wiring:** `rg "currentAnswer\\?\\.type === 'open'" src/routes/sessions/[id]/run/+page.svelte` and `rg "multiple-choice|true-false" src/routes/sessions/[id]/run/+page.svelte` each return ≥1 match (proves run page still dispatches by answer type).
    - `bun run check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run check</automated>
  </verify>
  <done>Run route documents UI requirements and remains free of a competing step authority (UI-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SessionEngine getters → Svelte derived state | UI must reflect engine-owned progression only |
| Teacher keybindings / click handlers → `recordOutcome` | Inputs map to outcomes without inventing step position |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | run UI step display | mitigate | No parallel step index; document UI-03; derive from engine getters only |
| T-04-02 | I | keyboard shortcuts for open answers | mitigate | Keep existing shortcut gating (open-only); no change to grading trust model in this plan |
| T-04-03 | D | misleading k-of-n if engine/load desync | mitigate | Tests on engine contract + explicit comment tying UI labels to `currentStepIndex`/`totalSteps` |

</threat_model>

<verification>
- `bun run check`
- `bun run test:unit -- --run src/lib/domain/session-engine/session-engine.test.ts`
</verification>

<success_criteria>
- UI-01: Shared stem (when present), current step, and k-of-n are consistent with engine state (tests + run page traceability).
- UI-02: Step answer types are honored via engine `currentStep` / `currentQuestion` data used by existing MC/TF/Open branches.
- UI-03: No UI-local authoritative step index; comment captures the invariant for future edits.
- Deferred D-02 (per-step `range` in `CodeBlock`) remains out of scope.
</success_criteria>

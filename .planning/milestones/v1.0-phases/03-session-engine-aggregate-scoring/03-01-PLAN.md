---
phase: 03-session-engine-aggregate-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/model/types.ts
  - src/lib/ports/session-engine-persistence.ts
  - src/lib/app/session-engine-persistence.ts
  - src/lib/domain/session-engine/index.ts
  - src/lib/domain/session-engine/session-engine.test.ts
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
must_haves:
  truths:
    - "A multi-step logical question is drawn once and consumes one slot only when first shown."
    - "SessionEngine exposes authoritative step progression as currentStepIndex, totalSteps, and currentStep."
    - "Aggregate outcomes resolve to correct, wrong, or partial using all-correct/all-wrong/mixed rules."
    - "Exactly one Attempt is persisted per completed logical unit using logical question_id."
    - "Session completion occurs only when every student has answered assigned logical questions, not just when the pool is exhausted."
  artifacts:
    - path: "src/lib/domain/session-engine/index.ts"
      provides: "Single-authority logical-unit progression and aggregate scoring"
    - path: "src/lib/ports/session-engine-persistence.ts"
      provides: "Persistence contract for resumable active logical unit state"
    - path: "src/lib/domain/session-engine/session-engine.test.ts"
      provides: "Regression tests for aggregate and one-attempt semantics"
  key_links:
    - from: "src/lib/domain/session-engine/index.ts"
      to: "src/lib/ports/session-engine-persistence.ts"
      via: "persistActiveUnitState / clearActiveUnitState"
      pattern: "persistActiveUnitState|clearActiveUnitState"
    - from: "src/lib/domain/session-engine/index.ts"
      to: "src/lib/model/types.ts"
      via: "session active_unit_progress state typing"
      pattern: "active_unit_progress"
---

<objective>
Implement SessionEngine as the sole authority for logical-unit progression and aggregate scoring, including resumable in-progress state.

Purpose: Satisfy Phase 3 core runtime guarantees for one-draw logical units, linear step progression, and one-attempt aggregate recording.
Output: Updated engine contracts/logic plus tests proving SESS-01..04 behavior.
</objective>

<execution_context>
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-session-engine-aggregate-scoring/03-CONTEXT.md
@src/lib/domain/session-engine/index.ts
@src/lib/model/types.ts
@src/lib/ports/session-engine-persistence.ts
@src/lib/app/session-engine-persistence.ts
@src/lib/domain/session-engine/session-engine.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define resumable logical-unit engine contracts</name>
  <read_first>
    src/lib/model/types.ts
    src/lib/ports/session-engine-persistence.ts
    src/lib/app/session-engine-persistence.ts
    .planning/phases/03-session-engine-aggregate-scoring/03-CONTEXT.md
  </read_first>
  <files>
    src/lib/model/types.ts
    src/lib/ports/session-engine-persistence.ts
    src/lib/app/session-engine-persistence.ts
  </files>
  <action>
    Add `active_unit_progress` to `Session` with exact shape:
    `{ root_question_id: string; step_index: number; step_outcomes: Array<'correct' | 'wrong'> } | null`.
    Extend `SessionEnginePersistence` with two methods:
    `persistActiveUnitState(sessionId: string, state: NonNullable<Session['active_unit_progress']>): Promise<void>`
    and `clearActiveUnitState(sessionId: string): Promise<void>`.
    Wire `src/lib/app/session-engine-persistence.ts` so these methods call `sessionRepository.updateSession(sessionId, { active_unit_progress: state })` and `sessionRepository.updateSession(sessionId, { active_unit_progress: null })`.
    Keep all existing method signatures unchanged. This implements D-02 and D-13 per locked decisions.
  </action>
  <acceptance_criteria>
    - `rg "active_unit_progress" src/lib/model/types.ts` finds Session field declaration.
    - `rg "persistActiveUnitState\\(|clearActiveUnitState\\(" src/lib/ports/session-engine-persistence.ts` returns both methods.
    - `rg "active_unit_progress: state|active_unit_progress: null" src/lib/app/session-engine-persistence.ts` returns both update paths.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:unit -- --run src/lib/domain/session-engine/session-engine.test.ts</automated>
  </verify>
  <done>Contracts compile and persistence adapter can store/clear active logical-unit progress.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite SessionEngine progression and aggregate outcome semantics</name>
  <read_first>
    src/lib/domain/session-engine/index.ts
    src/lib/domain/question-selector/registry.ts
    src/lib/domain/student-orderer/registry.ts
    src/lib/model/types.ts
    .planning/phases/03-session-engine-aggregate-scoring/03-CONTEXT.md
  </read_first>
  <files>
    src/lib/domain/session-engine/index.ts
  </files>
  <action>
    Replace chain-era synthetic-question behavior with logical-unit state:
    maintain `activeRootQuestion`, `currentStepIndex`, `stepOutcomes`, and derived getters `currentStep`, `currentStepIndex`, `totalSteps`.
    On first showing a root question, treat draw as consumed (D-01): do not redraw another root until the current logical unit is completed or skipped.
    Enforce linear forward-only progression (D-07): `recordOutcome` appends one step outcome and advances index by +1 only.
    Aggregate rules (D-08, D-09, D-10): all `correct` => `correct`; all `wrong` => `wrong`; any mix of `correct` and `wrong` => `partial`. Do not include step-level `partial` input in v1 scoring path.
    Persist exactly one Attempt at unit completion with `question_id` equal to logical root id and `outcome` equal to aggregate (D-11, D-12; SESS-04).
    Persist mid-unit state after each recorded step via `persistActiveUnitState` and clear it only when unit completes or is skipped (D-13).
    Keep one active student and one active logical unit at a time (D-15) and preserve existing strategy registry calls for next student/question selection (D-16, D-17 discretionary policy unchanged).
    Implement completion condition per D-14: session completes only after all students have answered their assigned logical questions; do not complete based solely on pool exhaustion.
  </action>
  <acceptance_criteria>
    - `rg "get currentStep\\(|get currentStepIndex\\(|get totalSteps\\(" src/lib/domain/session-engine/index.ts` returns all three getters.
    - `rg "persistActiveUnitState\\(|clearActiveUnitState\\(" src/lib/domain/session-engine/index.ts` shows mid-unit persistence and clear calls.
    - `rg "all.*correct|all.*wrong|partial" src/lib/domain/session-engine/index.ts` shows explicit aggregate mapping logic.
    - `rg "question_id: .*root" src/lib/domain/session-engine/index.ts` shows attempt recorded against logical root id.
    - `rg "assigned|unanswered|session complete|completion" src/lib/domain/session-engine/index.ts` shows completion logic tied to assignment coverage rather than pool exhaustion.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:unit -- --run src/lib/domain/session-engine/session-engine.test.ts</automated>
  </verify>
  <done>SessionEngine owns authoritative linear step progression, aggregates outcomes correctly, emits one Attempt per completed logical unit, and marks completion from per-student assignment coverage (D-14).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace chain-era tests with logical-unit aggregate/resume coverage</name>
  <read_first>
    src/lib/domain/session-engine/session-engine.test.ts
    src/lib/domain/session-engine/index.ts
    .planning/phases/03-session-engine-aggregate-scoring/03-CONTEXT.md
    .planning/REQUIREMENTS.md
  </read_first>
  <files>
    src/lib/domain/session-engine/session-engine.test.ts
  </files>
  <action>
    Rewrite tests to remove chain_parent assumptions and validate logical-unit behavior:
    (1) multi-step unit consumes one slot and emits one Attempt only after final step;
    (2) all-correct => `correct`;
    (3) all-wrong => `wrong`;
    (4) mixed correct/wrong => `partial`;
    (5) pause/reconstruct resumes same `root_question_id` and same `step_index` without creating draft Attempt;
    (6) skip mid-unit creates no Attempt and unit is not reinserted in the same session (D-03, D-04).
    Add explicit D-14 regression coverage:
    (7) session stays in progress when question pool is exhausted but any student still has unanswered assigned questions;
    (8) session marks complete only after every student has answered assigned logical questions.
    Use existing mock persistence style and keep `requireAssertions` compatibility.
  </action>
  <acceptance_criteria>
    - `rg "mixed.*partial|all.*correct|all.*wrong" src/lib/domain/session-engine/session-engine.test.ts` finds explicit matrix tests.
    - `rg "resume|paused|active_unit_progress" src/lib/domain/session-engine/session-engine.test.ts` finds resume-state tests.
    - `rg "skip" src/lib/domain/session-engine/session-engine.test.ts` finds skip/no-attempt behavior tests.
    - `rg "pool.*exhausted|assigned.*questions|all students" src/lib/domain/session-engine/session-engine.test.ts` finds D-14 completion-condition tests.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:unit -- --run src/lib/domain/session-engine/session-engine.test.ts</automated>
  </verify>
  <done>Test suite proves SESS-01/02/03/04 behaviors and locked decisions around skip and resume state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run UI -> SessionEngine methods | Untrusted user interactions trigger state transitions and persistence writes |
| SessionEngine -> persistence port | In-memory quiz state crosses into durable IndexedDB session records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `SessionEngine.recordOutcome` | mitigate | Guard transitions: ignore invalid calls when complete/no active unit; append exactly one step outcome per call; enforce bounds against totalSteps |
| T-03-02 | I | `active_unit_progress` in session | mitigate | Persist only root id, step index, and normalized outcomes (`correct`/`wrong`), no free-form payload; clear on completion/skip |
| T-03-03 | R | attempt creation path | mitigate | Emit one Attempt at completion only; assert no Attempt on pause/skip in tests to prevent duplicated scoring records |
</threat_model>

<verification>
- `bun run check`
- `bun run test:unit -- --run src/lib/domain/session-engine/session-engine.test.ts`
</verification>

<success_criteria>
- Session engine exposes and drives `currentStepIndex`, `totalSteps`, `currentStep` as the only progression authority.
- Multi-step logical unit produces exactly one Attempt with logical `question_id` and aggregate outcome.
- Mixed correct/wrong outcomes yield aggregate `partial`; all-correct and all-wrong map deterministically.
- Paused mid-unit state resumes at same root and step index with no draft attempts.
- Session completion is based on every student's assigned logical questions being answered, not only on question pool exhaustion.
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-engine-aggregate-scoring/03-01-SUMMARY.md`
</output>

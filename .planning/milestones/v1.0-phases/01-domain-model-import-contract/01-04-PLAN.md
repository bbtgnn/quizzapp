---
phase: 01-domain-model-import-contract
plan: 04
type: execute
wave: 4
depends_on:
  - "02"
  - "03"
files_modified:
  - src/lib/importer/question-sets.validation.test.ts
  - .github/workflows/ci.yml
  - package.json
autonomous: true
requirements:
  - IMPT-02
must_haves:
  truths:
    - "Automated test fails if any question-sets/**/*.json fails parseQuestionSetFile"
    - "CI workflow runs unit tests on push/PR (closes D-13 CI gap called out in RESEARCH)"
    - "If workflow cannot be added, acceptance criteria explicitly document the gap and require local/script gate"
  artifacts:
    - path: src/lib/importer/question-sets.validation.test.ts
      provides: "Glob validation of bundled JSON"
      contains: "question-sets"
    - path: .github/workflows/ci.yml
      provides: "GitHub Actions CI"
      contains: "test:unit|vitest"
  key_links:
    - from: "question-sets.validation.test.ts"
      to: "parseQuestionSetFile"
      via: "read file → parse"
      pattern: "parseQuestionSetFile"
---

<objective>
Add **automated verification (D-13)** that **every** `question-sets/**/*.json` validates through the **runtime importer**, and add **GitHub Actions** so CI enforces it (RESEARCH noted **no** `.github` workflows today).

Purpose: Locks IMPT-02 and D-13; prevents regressions to legacy on-disk shapes.

Output: `question-sets.validation.test.ts`, `.github/workflows/ci.yml`, optional npm script.
</objective>

<execution_context>
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-domain-model-import-contract/01-RESEARCH.md § Environment availability (CI gap)
@vite.config.ts (vitest server project include patterns)

Use **dynamic import** of `fs` + `path` in test, or `import.meta.glob` if Vite exposes bundled JSON — **preferred:** Node `fs.readdirSync` recursive from repo root `question-sets` because JSON is not under `src/`. Vitest server project runs in Node; use `fileURLToPath` + `path.join` to resolve `question-sets` relative to project root.

**D-13 wording:** “CI must verify” — deliver **workflow**; if organization blocks Actions, set `autonomous: false` is NOT allowed by user; instead implement workflow and note in SUMMARY that enabling Actions requires repo settings.
</context>

<decision_coverage>
| Decision | Task | Notes |
|----------|------|-------|
| D-13 | Task 1 | Vitest iterates all JSON under question-sets |
| D-13 | Task 2 | `ci.yml` runs `bun install` + `bun run test:unit -- --run` |
| IMPT-02 | Task 1 | Confirms bundled files remain loadable post-migration |
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner executing tests | Trusted pipeline; validates repository state, not end-user uploads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | D | `question-sets.validation.test.ts` | mitigate | Fail test on first invalid file with file path in assertion message. |
| T-04-02 | I | CI workflow | mitigate | Use pinned `actions/checkout@v4`; run tests without passing untrusted secrets to scripts. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add question-sets.validation.test.ts (D-13)</name>
  <read_first>
    - `vite.config.ts` (test projects, root)
    - `src/lib/importer/index.ts` (`parseQuestionSetFile`)
    - `01-RESEARCH.md` § Phase requirements → test map
  </read_first>
  <files>src/lib/importer/question-sets.validation.test.ts</files>
  <behavior>
    - Discovers every `question-sets/**/*.json` relative to repo root.
    - For each file: `parseQuestionSetFile(fs.readFileSync(path, 'utf8'))` expects `ok === true`.
    - If zero JSON files found, test fails (guard against glob breakage).
  </behavior>
  <action>
    Create `src/lib/importer/question-sets.validation.test.ts` using Vitest `describe`/`it` or single `it` loop.

    Resolve repo root via `path.resolve(__dirname, '../../..')` from file location **or** `process.cwd()` documented as “vitest runs with cwd = project root” — verify and pin the correct relative path to `question-sets/`.

    On failure, include **file path** in thrown message for quick fixes.
  </action>
  <acceptance_criteria>
    - `test -f src/lib/importer/question-sets.validation.test.ts`
    - `bun run test:unit -- --run src/lib/importer/question-sets.validation.test.ts` exits 0 after Plan 03 migration.
    - `rg "question-sets" src/lib/importer/question-sets.validation.test.ts` matches.
  </acceptance_criteria>
  <verify>
    <automated>bun run test:unit -- --run src/lib/importer/question-sets.validation.test.ts</automated>
  </verify>
  <done>Every bundled JSON file is covered by an automated parse check.</done>
</task>

<task type="auto">
  <name>Task 2: Add GitHub Actions workflow for unit tests</name>
  <read_first>
    - `package.json` scripts (`test:unit`, `check`)
    - Verify `.github` does not exist or is empty before adding
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    Create `.github/workflows/ci.yml`:
    - Triggers: `push` and `pull_request` on default branch (use `main` or detect from repo — if unknown, use `push`/`pull_request` without branch filter or use `**`).
    - Job on `ubuntu-latest`.
    - Steps: `actions/checkout@v4`, `oven-sh/setup-bun@v2` (or official Bun setup), `bun install --frozen-lockfile` if lockfile present else `bun install`, then `bun run test:unit -- --run`.

    Optionally chain `bun run check` as a second step or parallel job — **minimum** is unit tests including the new validation test.

    Add a YAML comment at top: “Closes Phase 1 D-13 — validates question-sets JSON in CI.”
  </action>
  <acceptance_criteria>
    - `test -f .github/workflows/ci.yml`
    - `rg "test:unit|vitest" .github/workflows/ci.yml` matches
    - `rg "checkout@v4" .github/workflows/ci.yml` matches
  </acceptance_criteria>
  <verify>
    <automated>test -s .github/workflows/ci.yml && grep -q "test:unit" .github/workflows/ci.yml && grep -q "on:" .github/workflows/ci.yml && grep -q "jobs:" .github/workflows/ci.yml</automated>
  </verify>
  <note>YAML syntax is validated when GitHub Actions runs; local check is content presence.</note>
  <done>CI workflow file exists and runs unit tests with Bun.</done>
</task>

<task type="auto">
  <name>Task 3: Optional package.json script + document CI assumption</name>
  <read_first>
    - `package.json`
  </read_first>
  <files>package.json</files>
  <action>
    Add script `"validate:question-sets": "vitest run src/lib/importer/question-sets.validation.test.ts"` **or** `bun run test:unit -- --run src/lib/importer/question-sets.validation.test.ts` — match existing test runner conventions.

    In plan executor SUMMARY (not a new repo doc), record: **Before Phase 1, `.github/` was absent**; workflow added satisfies D-13 CI requirement when Actions enabled on the remote.
  </action>
  <acceptance_criteria>
    - `rg "validate:question-sets|question-sets.validation" package.json` matches at least one line.
    - `bun run validate:question-sets` exits 0 (if script name is validate:question-sets).
  </acceptance_criteria>
  <verify>
    <automated>bun run validate:question-sets</automated>
  </verify>
  <done>One-command local repro for D-13; SUMMARY notes CI enablement.</done>
</task>

</tasks>

<verification>
`bun run test:unit -- --run src/lib/importer/` passes; `bun run validate:question-sets` passes; CI YAML present.
</verification>

<success_criteria>
D-13 automated coverage exists locally and in GitHub Actions; IMPT-02 guarded by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-model-import-contract/01-04-SUMMARY.md`
</output>

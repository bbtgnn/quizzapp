---
phase: 01-domain-model-import-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - src/lib/model/types.ts
  - src/lib/importer/question-set.schema.ts
  - src/lib/importer/errors.ts
autonomous: true
requirements:
  - MODL-01
  - MODL-02
must_haves:
  truths:
    - "Zod 4 is installed and importable from application code"
    - "Question-set file and logical-question shapes are expressible as z.strictObject trees with discriminated unions"
    - "Cross-field rules enforce step ranges for shared code-snippet and forbid ranges for markdown shared content"
    - "OpenAnswerConfig supports optional referenceAnswer for migrated open reference text (D-12)"
  artifacts:
    - path: package.json
      provides: "zod dependency (^4.x)"
      contains: '"zod"'
    - path: src/lib/importer/question-set.schema.ts
      provides: "Zod schemas + z.infer types for import boundary"
      contains: "strictObject"
    - path: src/lib/importer/errors.ts
      provides: "Zod issue → single path + reason string (D-08)"
      contains: "issues"
  key_links:
    - from: "src/lib/importer/question-set.schema.ts"
      to: "src/lib/model/types.ts"
      via: "AnswerConfig / ContentConfig alignment for discriminant type fields"
      pattern: "discriminatedUnion|literal\\('type'"
---

<objective>
Add **Zod 4** and implement the **strict import schemas** for Phase 1: file envelope `{ name, schemaVersion, questions }`, logical questions with optional `shared` and non-empty `steps`, per-step `text` / `answer` / optional `range`, plus **error formatting** for import failures.

Purpose: Establishes MODL-01/MODL-02 and the validation artifact IMPT-01 will call at runtime.

Output: `question-set.schema.ts`, `errors.ts`, `OpenAnswerConfig.referenceAnswer`, `package.json` + lockfile updated.
</objective>

<execution_context>
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/giovanniabbatepaolo/Documents/GitHub/quizzapp/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-domain-model-import-contract/01-CONTEXT.md
@.planning/phases/01-domain-model-import-contract/01-RESEARCH.md

<interfaces>
From `src/lib/model/types.ts` (extend, do not break discriminant names):

```typescript
export interface CodeSnippetContent {
	type: 'code-snippet';
	language: string;
	code: string;
	highlight?: { startLine: number; endLine: number };
}
export interface MarkdownContent {
	type: 'markdown';
	body: string;
}
export type ContentConfig = CodeSnippetContent | MarkdownContent;
export interface OpenAnswerConfig {
	type: 'open';
}
export type AnswerConfig = OpenAnswerConfig | MCAnswerConfig | TFAnswerConfig;
```

**Import-only shared `code-snippet` (D-05):** Zod schema for `shared.content` when `type === 'code-snippet'` must allow **only** `language` and `code` (no `highlight` key). Persisted `ContentConfig` may still carry `highlight` after the Phase 1 bridge maps `step.range` → `highlight`.
</interfaces>
</context>

<decision_coverage>
| Decision | Task | Notes |
|----------|------|-------|
| D-01 | Task 3 | Root `name`, `schemaVersion` (literal `1` for v1), `questions` array |
| D-02 | Task 3 | `shared?`, `steps` min 1; no `chain`; no root-level question `answer` outside steps |
| D-03 | Task 3 | Step: `text` min 1; optional `z.union([z.strictObject({ question: ... }), z.strictObject({ text: ... })])` with transform to canonical `text` **or** document single public field `text` only — pick one in implementation |
| D-04 | Task 3 | `answer.type` union: `open` \| `multiple-choice` \| `true-false` |
| D-05 | Task 3 | `superRefine`: if `shared?.content.type === 'code-snippet'`, every step must have `range` |
| D-06 | Task 3 | `superRefine`: if `shared?.content.type === 'markdown'`, steps must not include `range` |
| D-07 | Task 3 | Nested objects use `z.strictObject({...})` (not loose `z.object`) for validated nodes |
| D-08 | Task 2 | Formatter produces `questions[0].steps[1].answer`-style path + short message |
| D-09 | Task 3 | Export `z.infer<typeof QuestionSetFileSchema>` (or named aliases) for boundary types |
| D-12 | Task 1 | `referenceAnswer?: string` on `OpenAnswerConfig`; Zod `open` branch includes optional `referenceAnswer` |
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Uploaded / bundled JSON → parser | Untrusted structured input; must not reach persistence without schema validation (full mitigation in Plan 02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `question-set.schema.ts` | mitigate | Discriminated unions on `type` for `ContentConfig` and `AnswerConfig`; reject wrong-shaped branches before use. |
| T-01-02 | I | `question-set.schema.ts` | mitigate | `z.strictObject` on validated objects so unexpected keys fail parse (D-07 / V5). |
| T-01-03 | D | `errors.ts` | accept | Error strings derived from schema paths only; no secrets in question-set format. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend OpenAnswerConfig for referenceAnswer (D-12)</name>
  <read_first>
    - `src/lib/model/types.ts` (full file)
    - `rg "OpenAnswerConfig|type: 'open'" src/` for consumers
  </read_first>
  <files>src/lib/model/types.ts</files>
  <behavior>
    - Type `OpenAnswerConfig` includes optional `referenceAnswer?: string`.
    - Existing `{ type: 'open' }` values remain valid at compile time.
  </behavior>
  <action>
    In `src/lib/model/types.ts`, on `OpenAnswerConfig`, add `referenceAnswer?: string` with a one-line comment that it stores teacher reference text for open questions (D-12); do not change scoring behavior.
  </action>
  <acceptance_criteria>
    - `rg "referenceAnswer" src/lib/model/types.ts` returns at least one match inside `OpenAnswerConfig`.
    - `bun run check` exits 0 (or document and fix any new type errors in the same task).
  </acceptance_criteria>
  <verify>
    <automated>bun run check</automated>
  </verify>
  <done>OpenAnswerConfig documents and allows optional referenceAnswer; project typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add zod dependency (Zod 4)</name>
  <read_first>
    - `package.json`
    - `01-RESEARCH.md` § Standard stack (zod 4.x)
  </read_first>
  <files>package.json, bun.lock</files>
  <action>
    Run `bun add zod` at repo root. Confirm `package.json` `"dependencies"` contains `"zod"` with major version **4** (e.g. `^4.3.6` or latest 4.x from registry). Commit lockfile changes with the dependency.
  </action>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json'); console.log(p.dependencies.zod)"` prints a string starting with `^4` or `4.`
    - `bun -e "import { z } from 'zod'; console.log(typeof z.strictObject)"` prints `function`
  </acceptance_criteria>
  <verify>
    <automated>bun -e "import { z } from 'zod'; if (typeof z.strictObject !== 'function') process.exit(1)"</automated>
  </verify>
  <done>Zod 4 installed and resolvable.</done>
</task>

<task type="auto">
  <name>Task 3: Implement strict Zod schemas and error formatter</name>
  <read_first>
    - `src/lib/importer/index.ts` (existing content/answer validation expectations)
    - `01-CONTEXT.md` decisions D-01–D-09, D-12
    - Zod 4 docs: `z.strictObject`, `z.discriminatedUnion`, `safeParse`, `superRefine`
  </read_first>
  <files>src/lib/importer/question-set.schema.ts, src/lib/importer/errors.ts</files>
  <action>
    Create `src/lib/importer/question-set.schema.ts`:
    - Root `QuestionSetFileSchema` = `z.strictObject({ name: z.string().min(1), schemaVersion: z.literal(1), questions: z.array(LogicalQuestionSchema).min(1) })` per D-01.
    - `LogicalQuestionSchema`: `z.strictObject` with optional `shared: z.strictObject({ content: ContentConfigSchema })`, required `steps: z.array(StepSchema).min(1)`, and optional `difficulty: z.string()` only if still needed for parity; **no** `chain`, **no** top-level `text`/`content`/`answer` on the logical question object (D-02).
    - `StepSchema` = `z.strictObject({ text: z.string().min(1), answer: AnswerConfigSchema, range: z.strictObject({ startLine: z.number().int(), endLine: z.number().int() }).optional() })` (D-03, D-04).
    - `ContentConfigSchema`: `z.discriminatedUnion('type', [ markdown strictObject, code-snippet strictObject with only language+code ])` — **omit** `highlight` on import code-snippet (D-05).
    - `AnswerConfigSchema`: `z.discriminatedUnion('type', [ open with optional referenceAnswer, multiple-choice, true-false ])` aligned with `src/lib/model/types.ts` (D-04, D-12).
    - Attach `superRefine` on logical question: if `shared?.content.type === 'code-snippet'`, require every step to define `range`; if `shared?.content.type === 'markdown'`, forbid `range` on all steps (D-05, D-06).
    - Export inferred types, e.g. `export type QuestionSetFile = z.infer<typeof QuestionSetFileSchema>` (D-09).

    Create `src/lib/importer/errors.ts`:
    - Export `formatQuestionSetParseError(error: z.ZodError): string` that builds **one** line: JSON path like `questions[0].steps[1].answer` + short message from first issue (D-08). Map Zod `path` segments to bracket notation for numeric indices.
    - Optionally log full `error.issues` with `console.debug` in dev only if already using a dev flag pattern; otherwise skip.

    Add `src/lib/importer/index.ts` **re-exports** for `QuestionSetFileSchema` and `formatQuestionSetParseError` only if needed by tests/scripts; prefer direct imports from schema/errors files to avoid circular deps.
  </action>
  <acceptance_criteria>
    - `rg "strictObject" src/lib/importer/question-set.schema.ts | wc -l` is ≥ 5
    - `rg "discriminatedUnion\\('type'" src/lib/importer/question-set.schema.ts` matches at least twice (content + answer)
    - `rg "superRefine" src/lib/importer/question-set.schema.ts` matches at least once
    - `rg "formatQuestionSetParseError" src/lib/importer/errors.ts` matches at least once
  </acceptance_criteria>
  <verify>
    <automated>bun run check</automated>
  </verify>
  <note>Importer unit tests are updated in Plan 02; this plan only requires typecheck green.</note>
  <done>Schema and error modules compile; MODL-01/02 structural rules encoded in Zod.</done>
</task>

</tasks>

<verification>
`bun run check` passes after Task 3; schema file imports `z` from `zod` without type errors.
</verification>

<success_criteria>
Executable schemas enforce D-01–D-07, D-09, D-12 at compile time; D-08 formatter exists; Zod 4 present in dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-model-import-contract/01-01-SUMMARY.md`
</output>
